---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - next.config.mjs
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03, SETUP-04, SETUP-05]
must_haves:
  truths:
    - "npm install completes without errors"
    - "npm run dev boots the Next.js dev server without MODULE_NOT_FOUND errors"
    - "npm run lint runs Biome without schema or config errors"
    - "TypeScript path alias @/* resolves to ./src/*"
  artifacts:
    - path: "package.json"
      provides: "All dependencies matching Ghostty versions, scripts, overrides"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript config with path alias"
      contains: "@/*"
    - path: "biome.json"
      provides: "Biome linting and formatting config"
      contains: "biomejs.dev/schemas/2.4.4"
    - path: "next.config.mjs"
      provides: "MDX pipeline config with plugin require.resolve paths"
      contains: "createMDX"
  key_links:
    - from: "next.config.mjs"
      to: "src/lib/docs/*.mjs"
      via: "require.resolve()"
      pattern: "require\\.resolve.*src/lib/docs"
    - from: "tsconfig.json"
      to: "src/*"
      via: "paths alias"
      pattern: '"@/\\*".*"\\./src/\\*"'
---

<objective>
Create the project config foundation: package.json with all Ghostty-mirrored dependencies, tsconfig.json with path alias, biome.json for linting, and next.config.mjs with MDX pipeline. Also create the directory skeleton so Wave 2 can write files into src/.

Purpose: Establish the build toolchain so `npm install`, `npm run dev`, and `npm run lint` all work.
Output: 4 config files at project root, directory skeleton under src/, node_modules installed.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold/01-CONTEXT.md
@.planning/phases/01-project-scaffold/01-RESEARCH.md
@.planning/phases/01-project-scaffold/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config files and directory skeleton</name>
  <files>package.json, tsconfig.json, biome.json, next.config.mjs</files>
  <read_first>
    /Users/ruiwang/Developments/webank/aidx/.planning/phases/01-project-scaffold/01-PATTERNS.md
    /Users/ruiwang/Developments/ghostty-org/website/package.json
    /Users/ruiwang/Developments/ghostty-org/website/tsconfig.json
    /Users/ruiwang/Developments/ghostty-org/website/biome.json
    /Users/ruiwang/Developments/ghostty-org/website/next.config.mjs
  </read_first>
  <action>
1. Create directory skeleton:
   ```
   src/app/
   src/components/text/font/
   src/lib/docs/
   src/styles/
   src/types/
   ```

2. Replace `package.json` entirely (do NOT merge with existing canvas dep) with the exact content from 01-PATTERNS.md "package.json" section. Key values:
   - name: "aidx", version: "0.1.0", private: true
   - scripts: dev/build/start use next, lint uses "biome check .", format uses "biome format --write ."
   - dependencies: next ^16.1.6, react 19.2.4, react-dom 19.2.4, @next/mdx ^16.1.6, @mdx-js/loader ^3.1.1, remark-frontmatter ^5.0.0, remark-gfm ^4.0.1, remark-mdx ^3.1.1, remark-parse ^11.0.0, rehype-highlight ^7.0.2, @r4ai/remark-callout ^0.6.2, classnames ^2.5.1, fast-xml-parser ^5.3.7, gray-matter ^4.0.3, lucide-react ^0.575.0, mermaid ^11.12.3, react-intersection-observer ^10.0.3, slugify ^1.6.6, zustand ^5.0.11
   - devDependencies: @biomejs/biome ^2.4.4, @types/node ^25.3.0, @types/react 19.2.14, @types/react-dom 19.2.3, typescript ^5.9.3
   - CRITICAL: include overrides block pinning @types/react to 19.2.14 and @types/react-dom to 19.2.3 (per D-02, prevents React 19 peer dep conflicts)

3. Create `tsconfig.json` verbatim from 01-PATTERNS.md. Key: paths must have `"@/*": ["./src/*"]`. Include next plugin, jsx react-jsx, target ES2017, module esnext, moduleResolution bundler.

4. Create `biome.json` verbatim from 01-PATTERNS.md. Key: $schema must be "https://biomejs.dev/schemas/2.4.4/schema.json". Files includes: ["**/src/**/*.ts", "**/src/**/*.tsx", "**/next.config.mjs"]. Linter rules: recommended true, noForEach off, useExhaustiveDependencies off, noDangerouslySetInnerHtml off.

5. Create `next.config.mjs` verbatim from 01-PATTERNS.md. Uses createMDX from @next/mdx, createRequire from node:module. Three require.resolve() calls for plugin stubs at ./src/lib/docs/remark-gfm-alerts-as-callouts.mjs, ./src/lib/docs/remark-heading-ids.mjs, ./src/lib/docs/rehype-highlight-all.mjs. pageExtensions includes md/mdx. reactStrictMode true. experimental.largePageDataBytes = 2MB. env.GIT_COMMIT_REF from VERCEL_GIT_COMMIT_REF. Headers function adds X-Robots-Tag noindex for non-production.

6. Run `npm install`. Verify it completes without errors.
  </action>
  <verify>
    <automated>cd /Users/ruiwang/Developments/webank/aidx && node -e "const p=require('./package.json'); console.assert(p.name==='aidx'); console.assert(p.dependencies.next==='^16.1.6'); console.assert(p.overrides['@types/react']==='19.2.14'); console.log('package.json OK')" && node -e "const t=require('./tsconfig.json'); console.assert(t.compilerOptions.paths['@/*'][0]==='./src/*'); console.log('tsconfig OK')" && node -e "const b=require('./biome.json'); console.assert(b['$schema'].includes('2.4.4')); console.log('biome OK')" && test -f next.config.mjs && echo "next.config.mjs exists" && test -d node_modules && echo "node_modules exists"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "aidx"` and `"next": "^16.1.6"` and `"overrides"` block
    - tsconfig.json contains `"@/*": ["./src/*"]`
    - biome.json contains `"biomejs.dev/schemas/2.4.4"`
    - next.config.mjs contains `createMDX` and `require.resolve`
    - Directories exist: src/app/, src/components/text/font/, src/lib/docs/, src/styles/, src/types/
    - node_modules/ directory exists after npm install
    - npm install exits with code 0
  </acceptance_criteria>
  <done>All 4 config files created matching Ghostty source exactly (per D-01, D-02). Directory skeleton in place. npm install succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config → runtime | next.config.mjs env vars and headers affect served content |
| CDN → browser | Google Fonts loaded at build time via next/font (no runtime CDN call) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | next.config.mjs env block | mitigate | GIT_COMMIT_REF is non-sensitive build metadata only; no secrets in env block |
| T-01-02 | Information Disclosure | X-Robots-Tag header | mitigate | noindex header applied to non-production environments prevents search indexing of preview deploys |
| T-01-03 | Tampering | npm dependencies | accept | Using exact versions from verified Ghostty source; lockfile committed; no private registry |
</threat_model>

<verification>
1. `npm install` exits 0
2. `node -e "require('./package.json')"` parses without error
3. All src/ subdirectories exist
4. next.config.mjs references three plugin paths that will be created in Plan 02
</verification>

<success_criteria>
- npm install completes without errors
- All 4 config files exist at project root with correct content
- Directory skeleton created under src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md`
</output>
