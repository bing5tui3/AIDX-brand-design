---
phase: 06-eye-animation-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - terminals/home/animation_frames/frame_001.txt
  - terminals/home/animation_frames/frame_041.txt
  - terminals/home/animation_frames/frame_081.txt
  - terminals/home/animation_frames/frame_131.txt
  - terminals/home/animation_frames/frame_181.txt
  - src/app/HomeContent.tsx
  - .gitignore
autonomous: true
requirements:
  - EYE-01
  - EYE-02
  - EYE-03
  - EYE-04
  - EYE-05
  - EYE-06
  - EYE-07
  - EYE-08
  - EYE-09
  - EYE-10
  - EYE-11
  - EYE-12

must_haves:
  truths:
    - "All 235 frames modified by the script pass line-length validation (no column drift)"
    - "frame_001.txt still contains &gt; squint chars (stage 0, no change)"
    - "frame_041.txt contains <span class=\"e\">_</span> slit chars (stage 1)"
    - "frame_081.txt contains ( _ ) half-open shape in individual per-char spans (stage 2)"
    - "frame_131.txt contains ( o ) three-quarter shape in individual per-char spans (stage 3)"
    - "frame_181.txt contains ( O ) full-round shape in individual per-char spans (stage 4)"
    - "animation_frames_backup/ exists with 235 .txt files"
    - "animation_frames_backup/ is listed in .gitignore"
    - "HomeContent.tsx animationFrames array has .sort() before .map()"
    - "next build exits 0"
  artifacts:
    - path: "terminals/home/animation_frames_backup/"
      provides: "Backup of original 235 frames before modification"
      min_lines: 0
    - path: "terminals/home/animation_frames/frame_041.txt"
      provides: "Slit-stage frame — first frame with modified eye chars"
      contains: '<span class="e">_</span>'
    - path: "terminals/home/animation_frames/frame_181.txt"
      provides: "Full-round frame — first frame with ( O ) eye shape"
      contains: '<span class="e">O</span>'
    - path: "src/app/HomeContent.tsx"
      provides: "Defensive .sort() on animationFrames"
      contains: ".sort()"
  key_links:
    - from: "scripts/generate-eye-frames.js"
      to: "terminals/home/animation_frames/*.txt"
      via: "node scripts/generate-eye-frames.js"
      pattern: "Done. All frames validated successfully"
    - from: "terminals/home/animation_frames/*.txt"
      to: "next build"
      via: "loadAllTerminalFiles reads .txt at build time"
      pattern: "next build exits 0"
---

<objective>
Run scripts/generate-eye-frames.js to regenerate all 235 animation frames with the eye-opening progression, verify output integrity, add defensive .sort() to HomeContent.tsx, gitignore the backup directory, and confirm next build passes.

Purpose: Delivers the actual frame content changes that make the eye animation visible to users. Plan 01 wrote the tool; this plan uses it.
Output: 235 modified .txt frames with correct eye shapes per stage, passing next build.
</objective>

<execution_context>
@/Users/ruidemacbookair/Development/AIDX-brand-design/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruidemacbookair/Development/AIDX-brand-design/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ruidemacbookair/Development/AIDX-brand-design/.planning/PROJECT.md
@/Users/ruidemacbookair/Development/AIDX-brand-design/.planning/ROADMAP.md
@/Users/ruidemacbookair/Development/AIDX-brand-design/.planning/phases/06-eye-animation-enhancement/06-CONTEXT.md
@/Users/ruidemacbookair/Development/AIDX-brand-design/.planning/phases/06-eye-animation-enhancement/06-01-SUMMARY.md

<interfaces>
<!-- Key facts for verification commands. -->

Frame filename pattern: frame_NNN.txt (3-digit zero-padded, 1-indexed)
Frame count: 235 files total
Lines per frame: 48 (must be unchanged after script run)

Stage boundaries (for spot-check verification):
- frame_001.txt → stage 0 (squint) — must still contain &gt; chars, NO _ or ( O )
- frame_040.txt → stage 0 (squint) — last squint frame
- frame_041.txt → stage 1 (slit) — first frame with _ replacement
- frame_080.txt → stage 1 (slit) — last slit frame
- frame_081.txt → stage 2 (half-open) — first frame with ( _ ) shape
- frame_130.txt → stage 2 (half-open) — last half-open frame
- frame_131.txt → stage 3 (three-quarter) — first frame with ( o ) shape
- frame_180.txt → stage 3 (three-quarter) — last three-quarter frame
- frame_181.txt → stage 4 (full-round) — first frame with ( O ) shape
- frame_235.txt → stage 4 (full-round) — last frame

Blink frames that must be correctly overwritten (not skipped):
- frame_084.txt → stage 1 (slit) — was a blink frame with · chars, must now have _
- frame_097.txt → stage 1 (slit) — was a blink frame with _ chars, must now have _
- frame_161.txt → stage 3 (three-quarter) — was a blink frame with · chars, must now have ( o )
- frame_197.txt → stage 4 (full-round) — was a blink frame with · chars, must now have ( O )

HomeContent.tsx current state (line 32-34, no .sort()):
```typescript
const animationFrames = Object.keys(terminalData)
  .filter((k) => k.startsWith("home/animation_frames"))
  .map((k) => terminalData[k]);
```

Target state (with .sort()):
```typescript
const animationFrames = Object.keys(terminalData)
  .filter((k) => k.startsWith("home/animation_frames"))
  .sort()
  .map((k) => terminalData[k]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run generate-eye-frames.js and verify frame output</name>
  <files>
    terminals/home/animation_frames/frame_001.txt
    terminals/home/animation_frames/frame_041.txt
    terminals/home/animation_frames/frame_081.txt
    terminals/home/animation_frames/frame_084.txt
    terminals/home/animation_frames/frame_131.txt
    terminals/home/animation_frames/frame_161.txt
    terminals/home/animation_frames/frame_181.txt
    terminals/home/animation_frames/frame_197.txt
    terminals/home/animation_frames/frame_235.txt
    terminals/home/animation_frames_backup/
  </files>
  <read_first>
    - scripts/generate-eye-frames.js (confirm it exists and passes node --check before running)
    - terminals/home/animation_frames/frame_001.txt (confirm current state before running — should have &gt; chars)
    - terminals/home/animation_frames/frame_041.txt (confirm current state before running — should have &gt; chars, will become _)
  </read_first>
  <action>
Step 1 — Verify script is ready:
Run: `node --check /Users/ruidemacbookair/Development/AIDX-brand-design/scripts/generate-eye-frames.js`
If it fails, stop and report the syntax error. Do not proceed.

Step 2 — Run the script:
Run: `node /Users/ruidemacbookair/Development/AIDX-brand-design/scripts/generate-eye-frames.js`
Expected output: "Done. All frames validated successfully."
If the script exits non-zero or prints validation errors, stop and report all errors verbatim. Do not proceed to next steps.

Step 3 — Spot-check stage boundaries (run each grep, report any that fail):

Stage 0 (squint, no change) — frame_001.txt must still have &gt;:
`grep -c 'class="e">&gt;</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_001.txt`
Expected: >= 1

Stage 0 boundary — frame_001.txt must NOT have _ slit:
`grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_001.txt`
Expected: 0

Stage 1 (slit) — frame_041.txt must have _ chars:
`grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_041.txt`
Expected: >= 1

Stage 1 blink frame — frame_084.txt must have _ (was · before):
`grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_084.txt`
Expected: >= 1

Stage 2 (half-open) — frame_081.txt must have ( and _ and ) in separate spans:
`grep -c '<span class="e">(</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_081.txt`
Expected: >= 1
`grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_081.txt`
Expected: >= 1
`grep -c '<span class="e">)</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_081.txt`
Expected: >= 1

Stage 3 (three-quarter) — frame_131.txt must have ( o ) in separate spans:
`grep -c '<span class="e">o</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_131.txt`
Expected: >= 1

Stage 3 blink frame — frame_161.txt must have ( o ) (was · before):
`grep -c '<span class="e">o</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_161.txt`
Expected: >= 1

Stage 4 (full-round) — frame_181.txt must have ( O ) in separate spans:
`grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_181.txt`
Expected: >= 1

Stage 4 blink frame — frame_197.txt must have ( O ) (was · before):
`grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_197.txt`
Expected: >= 1

Stage 4 last frame — frame_235.txt must have ( O ):
`grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_235.txt`
Expected: >= 1

Step 4 — Verify line counts unchanged for spot-check frames:
`wc -l /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_041.txt`
Expected: 48 (or 47 if file has no trailing newline — compare against frame_001.txt which was not modified)
`wc -l /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_181.txt`
Expected: same as frame_001.txt

Step 5 — Verify backup exists:
`ls /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames_backup/ | wc -l`
Expected: 235
  </action>
  <verify>
    <automated>node /Users/ruidemacbookair/Development/AIDX-brand-design/scripts/generate-eye-frames.js && grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_041.txt && grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_181.txt</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 and prints "Done. All frames validated successfully."
    - frame_001.txt still contains &gt; squint chars: `grep -c 'class="e">&gt;</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_001.txt` returns >= 1
    - frame_001.txt has NO _ slit chars: `grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_001.txt` returns 0
    - frame_041.txt has _ slit chars: `grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_041.txt` returns >= 1
    - frame_084.txt (blink frame) has _ slit chars: `grep -c '<span class="e">_</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_084.txt` returns >= 1
    - frame_081.txt has ( half-open: `grep -c '<span class="e">(</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_081.txt` returns >= 1
    - frame_131.txt has o three-quarter: `grep -c '<span class="e">o</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_131.txt` returns >= 1
    - frame_161.txt (blink frame) has o three-quarter: `grep -c '<span class="e">o</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_161.txt` returns >= 1
    - frame_181.txt has O full-round: `grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_181.txt` returns >= 1
    - frame_197.txt (blink frame) has O full-round: `grep -c '<span class="e">O</span>' /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames/frame_197.txt` returns >= 1
    - Backup exists with 235 files: `ls /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames_backup/ | wc -l` returns 235
  </acceptance_criteria>
  <done>Script ran successfully, all spot-check frames contain the correct stage-appropriate eye chars, blink frames (084, 161, 197) are correctly overwritten, backup directory has 235 files.</done>
</task>

<task type="auto">
  <name>Task 2: Add .sort() to HomeContent.tsx, gitignore backup, run next build</name>
  <files>
    src/app/HomeContent.tsx
    .gitignore
  </files>
  <read_first>
    - src/app/HomeContent.tsx (read lines 31-35 to confirm current animationFrames construction before editing)
    - .gitignore (read full file to find correct insertion point for animation_frames_backup/)
  </read_first>
  <action>
Step 1 — Add .sort() to HomeContent.tsx:
Find the animationFrames construction block (lines 32-34):
```typescript
const animationFrames = Object.keys(terminalData)
  .filter((k) => k.startsWith("home/animation_frames"))
  .map((k) => terminalData[k]);
```
Change to:
```typescript
const animationFrames = Object.keys(terminalData)
  .filter((k) => k.startsWith("home/animation_frames"))
  .sort()
  .map((k) => terminalData[k]);
```
Insert `.sort()` as a new line between `.filter(...)` and `.map(...)`. Preserve indentation (2 spaces per level, matching existing code style).

Step 2 — Add animation_frames_backup/ to .gitignore:
Read .gitignore. Find the section for generated/build artifacts or local-only files. Add this line:
```
terminals/home/animation_frames_backup/
```
If no obvious section exists, append at the end of the file with a blank line separator.

Step 3 — Run next build:
Run: `cd /Users/ruidemacbookair/Development/AIDX-brand-design && npx next build`
Expected: exits 0 with no errors.
If build fails, read the error output carefully. Common causes:
- HTML entity corruption in frames (raw > or < in span content) — check validateFrame output
- TypeScript error in HomeContent.tsx from the .sort() edit — verify the edit is syntactically correct
Report the full error if build fails. Do not attempt to fix silently.
  </action>
  <verify>
    <automated>grep -n "\.sort()" /Users/ruidemacbookair/Development/AIDX-brand-design/src/app/HomeContent.tsx && grep "animation_frames_backup" /Users/ruidemacbookair/Development/AIDX-brand-design/.gitignore && cd /Users/ruidemacbookair/Development/AIDX-brand-design && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HomeContent.tsx has .sort() between .filter() and .map(): `grep -A2 'startsWith("home/animation_frames")' /Users/ruidemacbookair/Development/AIDX-brand-design/src/app/HomeContent.tsx` shows .sort() on the next line
    - .gitignore contains animation_frames_backup: `grep "animation_frames_backup" /Users/ruidemacbookair/Development/AIDX-brand-design/.gitignore` returns a match
    - next build exits 0: `cd /Users/ruidemacbookair/Development/AIDX-brand-design && npx next build` exits with code 0
    - No TypeScript errors in HomeContent.tsx: build output contains no error referencing HomeContent.tsx
    - animation_frames_backup/ is NOT tracked by git: `git status /Users/ruidemacbookair/Development/AIDX-brand-design/terminals/home/animation_frames_backup/` shows nothing to commit or directory not tracked
  </acceptance_criteria>
  <done>.sort() added to animationFrames in HomeContent.tsx, animation_frames_backup/ gitignored, next build exits 0 with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modified frames → Next.js build | 235 .txt files loaded as raw HTML at build time — malformed HTML would break the terminal renderer |
| backup dir → git | animation_frames_backup/ must not be committed — 235 extra files would bloat the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | Modified frames contain malformed HTML that breaks terminal renderer | mitigate | validateFrame() in Plan 01 script asserts span integrity and no raw angle brackets; next build exit code is the final gate |
| T-06-07 | Information Disclosure | animation_frames_backup/ committed to git, exposing 235 duplicate files | mitigate | Add terminals/home/animation_frames_backup/ to .gitignore in Task 2 |
| T-06-08 | Denial of Service | next build fails due to HTML entity corruption in regenerated frames | mitigate | Script validator runs before build; if validator passes and build still fails, error is reported verbatim for diagnosis |
| T-06-09 | Tampering | .sort() edit introduces TypeScript syntax error in HomeContent.tsx | mitigate | Edit is a single-line insertion; acceptance criteria includes next build exit 0 which catches TypeScript errors |
| T-06-10 | Elevation of Privilege | n/a — no auth, no network, no user input | accept | Local build-time operations only |
</threat_model>

<verification>
- `node scripts/generate-eye-frames.js` exits 0 and prints "Done. All frames validated successfully."
- frame_001.txt: still has &gt; squint chars, no _ or O
- frame_041.txt: has `<span class="e">_</span>` (slit stage)
- frame_081.txt: has `<span class="e">(</span>` and `<span class="e">_</span>` and `<span class="e">)</span>` (half-open)
- frame_131.txt: has `<span class="e">o</span>` (three-quarter)
- frame_181.txt: has `<span class="e">O</span>` (full-round)
- frame_084.txt (blink): has `<span class="e">_</span>` (correctly overwritten)
- frame_161.txt (blink): has `<span class="e">o</span>` (correctly overwritten)
- frame_197.txt (blink): has `<span class="e">O</span>` (correctly overwritten)
- animation_frames_backup/ has 235 files
- .gitignore contains terminals/home/animation_frames_backup/
- HomeContent.tsx has .sort() between .filter() and .map()
- `npx next build` exits 0
</verification>

<success_criteria>
All 235 frames regenerated with correct eye shapes per stage schedule, blink frames correctly overwritten, line lengths unchanged (validated by script), HomeContent.tsx has defensive .sort(), backup gitignored, next build exits 0. The homepage animation now shows eyes evolving from squint to round circles across the 235-frame loop.
</success_criteria>

<output>
After completion, create `.planning/phases/06-eye-animation-enhancement/06-02-SUMMARY.md`
</output>
