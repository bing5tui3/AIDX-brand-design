---
phase: quick-260611-fk8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/HomeContent.tsx]
autonomous: true
requirements: [QUICK-260611-FK8]

must_haves:
  truths:
    - "Homepage primary CTA reads 'Brand Design' and navigates to /brand-report.html in the same tab"
    - "Primary CTA keeps the brand-themed (default) ButtonLink styling"
    - "Navbar/footer 'Get Started' CTA in src/app/layout.tsx is unchanged"
  artifacts:
    - path: "src/app/HomeContent.tsx"
      provides: "Homepage CTA row with Brand Design primary button"
      contains: 'href="/brand-report.html"'
  key_links:
    - from: "src/app/HomeContent.tsx"
      to: "public/brand-report.html"
      via: "ButtonLink href (Link treats '/'-prefixed hrefs as internal, same-tab)"
      pattern: 'href="/brand-report\.html"'
---

<objective>
Replace the homepage "Get Started" primary CTA with a "Brand Design" button linking to the static Brand Design Page at /brand-report.html.

Purpose: The "Get Started" CTA is retired (href="#", dead link). The Brand Design Page (public/brand-report.html) is the primary destination; since the homepage renders without chrome (navbar/footer), the link must live in the homepage content itself.
Output: Updated src/app/HomeContent.tsx with the new primary CTA.
</objective>

<execution_context>
@/Users/ruidemacbookair/Development/AIDX-brand-design/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruidemacbookair/Development/AIDX-brand-design/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./CONTEXT.md
@src/app/HomeContent.tsx

<interfaces>
From src/app/HomeContent.tsx (lines 75-83), current CTA row:

```tsx
<GridContainer className={s.buttonsList}>
  <ButtonLink href="#" text="Get Started" size="large" />
  <ButtonLink
    href="/docs"
    text="Documentation"
    size="large"
    theme="neutral"
  />
</GridContainer>
```

ButtonLink (from @/components/link): no `theme` prop = brand-themed primary styling. The underlying Link component treats "/"-prefixed hrefs as internal, rendering same-tab navigation — no component changes required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Get Started CTA with Brand Design link</name>
  <files>src/app/HomeContent.tsx</files>
  <action>
    In src/app/HomeContent.tsx, change the first ButtonLink in the buttonsList GridContainer (line 76) from `href="#" text="Get Started"` to `href="/brand-report.html" text="Brand Design"`. Keep `size="large"` and do NOT add a `theme` prop (absence of theme = brand-themed primary styling, per locked decision). Do NOT add `target="_blank"` or any external-link handling — same-tab navigation is the locked decision, and the Link component already treats "/"-prefixed hrefs as internal. Leave the neutral Documentation button untouched. Scope is this file only — do NOT modify the navbar/footer "Get Started" CTA in src/app/layout.tsx.
  </action>
  <verify>
    <automated>grep -n 'href="/brand-report.html"' src/app/HomeContent.tsx && grep -n 'text="Brand Design"' src/app/HomeContent.tsx && ! grep -n 'Get Started' src/app/HomeContent.tsx && npm run lint</automated>
  </verify>
  <done>
    src/app/HomeContent.tsx contains a ButtonLink with href="/brand-report.html" and text="Brand Design" (no theme prop, size="large"); "Get Started" no longer appears in HomeContent.tsx; src/app/layout.tsx is untouched; biome check passes.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'Get Started' src/app/HomeContent.tsx` returns 0
- `grep 'Get Started' src/app/layout.tsx` still finds the chrome CTA (unchanged)
- `npm run lint` passes
- `npm run build` succeeds (static export unaffected — public/brand-report.html already exists)
</verification>

<success_criteria>
- Homepage primary CTA is "Brand Design" linking to /brand-report.html, same tab, brand-themed styling
- Neutral "Documentation" button unchanged
- No changes outside src/app/HomeContent.tsx
</success_criteria>

<output>
After completion, create `.planning/quick/260611-fk8-get-started-brand-design-brand-report-ht/260611-fk8-SUMMARY.md`
</output>
