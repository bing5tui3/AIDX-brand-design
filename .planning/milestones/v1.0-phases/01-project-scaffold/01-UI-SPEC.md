---
phase: 1
slug: project-scaffold
status: approved
shadcn_initialized: false
preset: none
created: 2026-04-16
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Project Scaffold.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
>
> **Scope note:** Phase 1 delivers zero rendered UI — no pages, no components.
> This contract governs the design token layer only: CSS custom properties,
> font loading, and the type system that Phase 2 components consume.
> All values are pre-populated from upstream artifacts (CONTEXT.md, REQUIREMENTS.md,
> Ghostty globals.css reference).

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — CSS Modules only | CONTEXT.md D-01, Ghostty mirror constraint |
| Preset | not applicable | — |
| Component library | none (Phase 2) | REQUIREMENTS.md traceability |
| Icon library | lucide-react (Phase 2) | REQUIREMENTS.md SETUP-02 |
| Body font | Pretendard Std Variable (local woff2) | CONTEXT.md D-04, REQUIREMENTS.md FONT-01 |
| Mono font | JetBrains Mono (Google Fonts) | CONTEXT.md D-05, REQUIREMENTS.md FONT-02 |

shadcn gate result: No `components.json` found. Stack is Next.js/CSS Modules mirroring
Ghostty architecture — shadcn is explicitly out of scope per CONTEXT.md D-01. Registry
safety gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4). Phase 1 does not render layout — these tokens are
defined in `globals.css` for Phase 2 consumption.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none. Touch targets (44px) are a Phase 2 concern.

---

## Typography

Font sizes are defined as CSS custom properties in `globals.css`. Phase 2 `Text`
component consumes them.

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Body | 16px | 400 | 1.5 | Pretendard Std Variable |
| Label / small | 14px | 400 | 1.4 | Pretendard Std Variable |
| Heading | 20px | 600 | 1.2 | Pretendard Std Variable |
| Display | 28px | 600 | 1.1 | Pretendard Std Variable |
| Code / mono | 14px | 400 | 1.6 | JetBrains Mono |

Weights declared: regular (400) + semibold (600). No other weights.

---

## Color

All values defined as CSS custom properties in `src/styles/globals.css`.
Brand color swap: Ghostty `#3551F3` → AIDX `#3A5ECF` (source: CONTEXT.md specifics).

| Role | Value | Usage | Source |
|------|-------|-------|--------|
| Dominant (60%) | `#0F0F11` (`--gray-0`) | Page background | Ghostty globals.css |
| Secondary (30%) | `#16161A` (`--gray-1`) | Blocks, cards, code bg | Ghostty globals.css |
| Accent (10%) | `#3A5ECF` (`--brand-color`) | See reserved list below | CONTEXT.md / REQUIREMENTS.md STYLE-01 |
| Destructive | `#E06C75` (`--atom-one-red`) | Destructive actions only | Ghostty globals.css |

Accent (`--brand-color: #3A5ECF`) reserved for:
- Text selection highlight (`::selection` background at 60% opacity)
- Primary CTA button background (Phase 2)
- Active nav link indicator (Phase 2)
- Inline link hover state (Phase 2)

Full gray scale (defined verbatim from Ghostty, background only changes):

| Token | Value | Semantic role |
|-------|-------|---------------|
| `--gray-0` | `#0F0F11` | Background |
| `--gray-1` | `#16161A` | Blocks |
| `--gray-2` | `#242428` | HRs |
| `--gray-3` | `#4C4C50` | Unfocused font, code background |
| `--gray-4` | `#828286` | Body font |
| `--gray-5` | `#959597` | — |
| `--gray-6` | `#C3C3C4` | — |
| `--gray-7` | `#DBDBDC` | — |
| `--gray-8` | `#EAEAEB` | — |
| `--gray-9` | `#F0F0F4` | Header font |

Atom-one syntax highlight tokens (defined in `globals.css`, consumed by `.hljs-*` classes):

| Token | Value |
|-------|-------|
| `--atom-one-blue` | `#61AEEE` |
| `--atom-one-green` | `#98C379` |
| `--atom-one-yellow` | `#E6C07B` |
| `--atom-one-purple` | `#C678DD` |
| `--atom-one-red` | `#E06C75` |
| `--atom-one-orange` | `#D19A66` |
| `--atom-one-teal` | `#56B6C2` |
| `--atom-one-gray` | `#5C6370` |
| `--atom-one-white` | `#ABB2BF` |

Callout semantic aliases:

| Token | Maps to |
|-------|---------|
| `--callout-note` | `--atom-one-blue` |
| `--callout-tip` | `--atom-one-green` |
| `--callout-important` | `--atom-one-purple` |
| `--callout-warning` | `--atom-one-yellow` |
| `--callout-caution` | `--atom-one-red` |

Print mode: `@media print` inverts gray scale (lightness flip). Atom-one colors shift
to print-safe dark-on-white equivalents. Defined verbatim from Ghostty globals.css.

---

## Layout Tokens

Defined in `globals.css` on `html, body`:

| Token | Value | Usage |
|-------|-------|-------|
| `--header-height` | `72px` | Sticky positioning calculations |
| `--footer-height` | `87px` | Layout offset calculations |
| `--form-border-radius` | `6px` | Form element rounding |
| `--brand-color-hsl` | `240 100% 45%` (AIDX-adjusted) | HSL variant for color-mix |

Note: `--brand-color-hsl` for AIDX `#3A5ECF` is approximately `225 62% 51%`. Executor
must recalculate from the actual hex rather than copying Ghostty's `240 100% 45%`.

---

## Copywriting Contract

Phase 1 has no rendered UI. No user-facing copy is produced in this phase.

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA | n/a — Phase 3 | Homepage CTA defined in Phase 3 |
| Empty state | n/a — Phase 1 has no data views | — |
| Error state | n/a — Phase 1 has no error surfaces | — |
| Destructive confirmation | n/a — no destructive actions in scaffold | — |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| third-party | none declared | not applicable |

No third-party registries. CSS Modules only. Registry vetting gate: not required.

---

## Phase 1 Deliverables Checklist (for executor reference)

Files this contract governs:

- `src/styles/globals.css` — all CSS custom properties above, `.hljs-*` classes, `@media print` block
- `src/components/text/font/pretendard-std-variable.woff2` — copied from Ghostty local clone
- `src/types/style.ts` — `Unit`, `UnitProp`, `SpacingProp` types (copied from Ghostty)
- `package.json`, `next.config.mjs`, `tsconfig.json`, `biome.json` — config files (no UI contract)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (no copy in Phase 1 — trivially satisfied)
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
