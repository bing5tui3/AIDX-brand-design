---
phase: 03-pages-docs-build
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/not-found.tsx
  - src/app/404Page.module.css
  - src/app/docs/[[...path]]/page.tsx
  - src/app/docs/DocsPageContent.tsx
  - src/app/docs/DocsPage.module.css
  - mdx-components.tsx
autonomous: true
requirements: [PAGE-05, PAGE-06, PAGE-07, PAGE-08]

must_haves:
  truths:
    - "Docs catch-all route generates static params for all MDX pages"
    - "DocsPageContent renders sidebar nav, content area, sidecar TOC, and edit link"
    - "404 page renders without images and exports force-static"
    - "MDX component map maps headings to JumplinkHeader, pre to CodeBlock, blockquote to Blockquote, and Callout variants"
  artifacts:
    - path: "src/app/docs/[[...path]]/page.tsx"
      provides: "Docs catch-all route with generateStaticParams"
      contains: "dynamicParams = false"
    - path: "src/app/docs/DocsPageContent.tsx"
      provides: "Docs layout with sidebar, content, sidecar"
      contains: "DocsPageContent"
    - path: "src/app/docs/DocsPage.module.css"
      provides: "Docs page layout styles"
      contains: ".docsPage"
    - path: "src/app/not-found.tsx"
      provides: "404 page without image"
      contains: "force-static"
    - path: "src/app/404Page.module.css"
      provides: "404 page styles"
      contains: ".notFoundPage"
    - path: "mdx-components.tsx"
      provides: "MDX element to component map"
      exports: ["useMDXComponents"]
  key_links:
    - from: "src/app/docs/[[...path]]/page.tsx"
      to: "@/lib/docs/page"
      via: "import loadDocsPage, loadAllDocsPageSlugs"
      pattern: "loadDocsPage"
    - from: "src/app/docs/[[...path]]/page.tsx"
      to: "src/app/docs/DocsPageContent.tsx"
      via: "import DocsPageContent"
      pattern: "DocsPageContent"
    - from: "mdx-components.tsx"
      to: "@/components/jumplink-header"
      via: "h1-h6 mapping"
      pattern: "JumplinkHeader"
---

<objective>
Create the docs routing pages, 404 page, and MDX component map — completing all page-level files that aren't the homepage.

Purpose: Enables /docs/* routes with sidebar navigation, renders MDX content through the component map, and provides a 404 fallback.
Output: 6 files — docs route, docs layout, docs styles, 404 page + styles, MDX component map.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-docs-build/03-PATTERNS.md
@.planning/phases/03-pages-docs-build/03-RESEARCH.md

<interfaces>
From src/lib/docs/config.ts:
```typescript
export const DOCS_DIRECTORY = "./docs";
export const DOCS_PAGES_ROOT_PATH = "/docs";
export const GITHUB_REPO_URL = "https://github.com/your-org/aidx";
```

From src/lib/docs/navigation.ts:
```typescript
export async function loadDocsNavTreeData(docsDir: string, activePath: string): Promise<NavTreeNode[]>;
export function docsMetadataTitle(breadcrumbs: Breadcrumb[]): string;
export function navTreeToBreadcrumbs(rootTitle: string, rootPath: string, navTree: NavTreeNode[], activeSlug: string): Breadcrumb[];
```

From src/lib/docs/page.ts:
```typescript
export interface DocsPageData { content: MDXContent; frontmatter: Record<string, unknown>; headers: PageHeader[]; }
export async function loadDocsPage(docsDir: string, slug: string): Promise<DocsPageData>;
export async function loadAllDocsPageSlugs(docsDir: string): Promise<string[]>;
```

From src/components/text/index.tsx:
```typescript
export { H1, H2, P, BodyParagraph, LI };
```

From src/components/jumplink-header/index.tsx:
```typescript
export default function JumplinkHeader(props: { as: "h1"|"h2"|"h3"|"h4"|"h5"|"h6"; children: ReactNode; id?: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 404 page, docs styles, and MDX component map</name>
  <files>src/app/not-found.tsx, src/app/404Page.module.css, src/app/docs/DocsPage.module.css, mdx-components.tsx</files>
  <read_first>
    ~/Developments/ghostty-org/website/src/app/not-found.tsx
    ~/Developments/ghostty-org/website/src/app/404Page.module.css
    ~/Developments/ghostty-org/website/src/app/docs/DocsPage.module.css
    ~/Developments/ghostty-org/website/mdx-components.tsx
    .planning/phases/03-pages-docs-build/03-PATTERNS.md
    src/components/jumplink-header/index.tsx
    src/components/codeblock/index.tsx
    src/components/callout/index.tsx
    src/components/blockquote/index.tsx
    src/components/text/index.tsx
    src/lib/docs/docs-mdx.module.css
  </read_first>
  <action>
    Create four files:

    **src/app/not-found.tsx** — Do NOT copy Ghostty version (it uses next/image with missing PNG). Create simplified AIDX version per RESEARCH.md Pattern 4:
    - `export const dynamic = "force-static"` (required for static export — BUILD-03)
    - metadata: `title: "Page not found | AIDX"`, `description: "We couldn't find what you were looking for."`
    - Render `<main>` with `<H2>This page could not be found.</H2>` and `<P>Try browsing the <a href="/docs">documentation</a>.</P>`
    - Import `H2, P` from `@/components/text` and `s` from `./404Page.module.css`

    **src/app/404Page.module.css** — Copy verbatim from Ghostty `~/Developments/ghostty-org/website/src/app/404Page.module.css`. Unused `.image` and `.imageCopyright` classes are harmless.

    **src/app/docs/DocsPage.module.css** — Copy verbatim from Ghostty `~/Developments/ghostty-org/website/src/app/docs/DocsPage.module.css`. No AIDX-specific changes.

    **mdx-components.tsx** (repo root) — Do NOT copy Ghostty version (it imports v2 components that don't exist). Create AIDX v1 subset per RESEARCH.md Pattern 5 and 03-PATTERNS.md:
    - Import: `JumplinkHeader`, `CodeBlock`, `Callout` (+ `Caution, Important, Note, Tip, Warning`), `Blockquote`, `BodyParagraph`, `LI` from Phase 2 components
    - Import: `s from "@/lib/docs/docs-mdx.module.css"` and `type { MDXComponents } from "mdx/types"` and `type { ComponentPropsWithoutRef } from "react"`
    - Map h1-h6 to `JumplinkHeader` with `as` prop, `li` to `LI`, `p` to `BodyParagraph`, `pre` to `CodeBlock`, `blockquote` to `Blockquote`
    - Include `Callout, Note, Tip, Important, Warning, Caution` and `"callout-title": () => null`
    - Export `useMDXComponents` function
  </action>
  <acceptance_criteria>
    - `grep 'force-static' src/app/not-found.tsx` matches
    - `grep 'Page not found | AIDX' src/app/not-found.tsx` matches
    - `grep -c "next/image" src/app/not-found.tsx` returns 0
    - `grep '.notFoundPage' src/app/404Page.module.css` matches
    - `grep '.docsPage' src/app/docs/DocsPage.module.css` matches
    - `grep 'useMDXComponents' mdx-components.tsx` matches
    - `grep 'JumplinkHeader' mdx-components.tsx` matches
    - `grep 'CodeBlock' mdx-components.tsx` matches
    - `grep 'Callout' mdx-components.tsx` matches
    - `grep -c "CardLinks\|ButtonLinks\|Mermaid\|VTSequence\|Video\|DonateCard\|SponsorBanner" mdx-components.tsx` returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep 'force-static' src/app/not-found.tsx && grep -c "next/image" src/app/not-found.tsx | grep "^0$" && grep 'useMDXComponents' mdx-components.tsx && grep -c "CardLinks" mdx-components.tsx | grep "^0$"</automated>
  </verify>
  <done>404 page renders text-only with force-static. MDX component map uses AIDX v1 subset only (no v2 imports). Docs page styles in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs catch-all route and DocsPageContent</name>
  <files>src/app/docs/[[...path]]/page.tsx, src/app/docs/DocsPageContent.tsx</files>
  <read_first>
    ~/Developments/ghostty-org/website/src/app/docs/[[...path]]/page.tsx
    ~/Developments/ghostty-org/website/src/app/docs/DocsPageContent.tsx
    .planning/phases/03-pages-docs-build/03-PATTERNS.md
    src/lib/docs/config.ts
    src/lib/docs/navigation.ts
    src/lib/docs/page.ts
    src/components/breadcrumbs/index.tsx
    src/components/nav-tree/index.tsx
    src/components/sidecar/index.tsx
    src/components/scroll-to-top/index.tsx
    src/components/text/index.tsx
    src/app/docs/DocsPage.module.css
  </read_first>
  <action>
    Create two files by copying from Ghostty source with documented deltas:

    **src/app/docs/[[...path]]/page.tsx** — Copy verbatim from Ghostty, apply one string swap:
    - `export const dynamicParams = false;` — keep verbatim (required for static export — BUILD-03)
    - `generateStaticParams` — keep verbatim (uses `loadAllDocsPageSlugs`)
    - `navTreeToBreadcrumbs("AIDX Docs", ...)` — change first arg from `"Ghostty Docs"` to `"AIDX Docs"` (per D-08)
    - `const { path } = await params;` — keep verbatim (Next.js 15 async params pattern)
    - All imports from `@/lib/docs/config`, `@/lib/docs/navigation`, `@/lib/docs/page` — keep verbatim

    **src/app/docs/DocsPageContent.tsx** — Copy verbatim from Ghostty `~/Developments/ghostty-org/website/src/app/docs/DocsPageContent.tsx`. No AIDX-specific changes needed. All imports (`Breadcrumbs`, `NavTree`, `ScrollToTopButton`, `Sidecar`, `H1`, `P`, `DOCS_PAGES_ROOT_PATH`, `GITHUB_REPO_URL`, `Pencil` from lucide-react) exist in AIDX Phase 2.
  </action>
  <acceptance_criteria>
    - `grep 'dynamicParams = false' src/app/docs/\\[\\[...path\\]\\]/page.tsx` matches
    - `grep 'generateStaticParams' src/app/docs/\\[\\[...path\\]\\]/page.tsx` matches
    - `grep 'AIDX Docs' src/app/docs/\\[\\[...path\\]\\]/page.tsx` matches
    - `grep -c 'Ghostty' src/app/docs/\\[\\[...path\\]\\]/page.tsx` returns 0
    - `grep 'await params' src/app/docs/\\[\\[...path\\]\\]/page.tsx` matches
    - `grep 'DocsPageContent' src/app/docs/DocsPageContent.tsx` matches
    - `grep 'GITHUB_REPO_URL' src/app/docs/DocsPageContent.tsx` matches
    - `grep 'NavTree' src/app/docs/DocsPageContent.tsx` matches
    - `grep 'Sidecar' src/app/docs/DocsPageContent.tsx` matches
  </acceptance_criteria>
  <verify>
    <automated>grep 'dynamicParams = false' "src/app/docs/[[...path]]/page.tsx" && grep 'AIDX Docs' "src/app/docs/[[...path]]/page.tsx" && grep 'DocsPageContent' src/app/docs/DocsPageContent.tsx</automated>
  </verify>
  <done>Docs catch-all route generates static params, uses "AIDX Docs" breadcrumb title, and renders DocsPageContent with sidebar nav, sidecar TOC, and edit link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Docs pages are statically generated at build time from local MDX files. No user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | I (Information Disclosure) | DocsPageContent edit link | accept | Links to public GitHub repo URL (placeholder). No secrets exposed. |
</threat_model>

<verification>
- `ls src/app/docs/[[...path]]/page.tsx` exists
- `ls src/app/docs/DocsPageContent.tsx` exists
- `ls src/app/docs/DocsPage.module.css` exists
- `ls src/app/not-found.tsx` exists
- `ls mdx-components.tsx` exists
- No v2 component imports in mdx-components.tsx
- No Ghostty references in any file
</verification>

<success_criteria>
Docs route, docs layout, 404 page, and MDX component map are all in place. Docs route uses dynamicParams=false for static export. 404 uses force-static with no image. MDX map uses only AIDX v1 components.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-docs-build/03-02-SUMMARY.md`
</output>
