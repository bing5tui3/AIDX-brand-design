---
phase: 03-pages-docs-build
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/nav.json
  - docs/index.mdx
  - docs/quick-start.mdx
  - public/favicon.svg
  - public/aidx-logo.svg
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, ASSET-01, ASSET-02, ASSET-03]

must_haves:
  truths:
    - "docs/nav.json defines one section 'Getting Started' with Overview and Quick Start pages"
    - "docs/index.mdx has frontmatter with title 'AIDX Docs' and hideSidecar: true"
    - "docs/quick-start.mdx demonstrates headings, code blocks, and a callout"
    - "public/favicon.svg exists as an SVG file"
    - "public/aidx-logo.svg exists as an SVG file"
  artifacts:
    - path: "docs/nav.json"
      provides: "Navigation tree structure"
      contains: "Getting Started"
    - path: "docs/index.mdx"
      provides: "Docs landing page"
      contains: "AIDX Docs"
    - path: "docs/quick-start.mdx"
      provides: "Quick start page with headings, code, callout"
      contains: "Quick Start"
    - path: "public/favicon.svg"
      provides: "SVG favicon"
      contains: "svg"
    - path: "public/aidx-logo.svg"
      provides: "AIDX logo SVG"
      contains: "svg"
  key_links:
    - from: "docs/nav.json"
      to: "docs/index.mdx"
      via: "path '/' maps to index.mdx"
      pattern: '"path": "/"'
    - from: "docs/nav.json"
      to: "docs/quick-start.mdx"
      via: "path '/quick-start' maps to quick-start.mdx"
      pattern: '"path": "/quick-start"'
---

<objective>
Create docs content files and copy public assets — the content layer that the page routes consume.

Purpose: Provides the MDX content for docs pages and the favicon/logo assets referenced by the layout metadata.
Output: 3 docs files (nav.json, index.mdx, quick-start.mdx) + 2 public assets (favicon.svg, aidx-logo.svg).
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-docs-build/03-RESEARCH.md
@.planning/phases/03-pages-docs-build/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs content files</name>
  <files>docs/nav.json, docs/index.mdx, docs/quick-start.mdx</files>
  <read_first>
    .planning/phases/03-pages-docs-build/03-RESEARCH.md
    .planning/phases/03-pages-docs-build/03-PATTERNS.md
  </read_first>
  <action>
    Create the `docs/` directory and three files per D-06/D-07/D-08/D-09:

    **docs/nav.json** (per D-07):
    ```json
    {
      "items": [
        {
          "type": "folder",
          "path": "/getting-started",
          "title": "Getting Started",
          "children": [
            { "type": "link", "path": "/", "title": "Overview" },
            { "type": "link", "path": "/quick-start", "title": "Quick Start" }
          ]
        }
      ]
    }
    ```

    **docs/index.mdx** (per D-08):
    ```mdx
    ---
    title: AIDX Docs
    description: AI-powered Developer Experience Platform
    hideSidecar: true
    ---

    Welcome to the AIDX documentation.

    AIDX is an AI-powered developer experience platform that accelerates your development workflow with intelligent tools.

    ## Get Started

    - [Quick Start](/docs/quick-start) — Get up and running in minutes
    ```

    **docs/quick-start.mdx** (per D-09 — must demonstrate headings, code block, and callout for DOCS-03):
    ```mdx
    ---
    title: Quick Start
    description: Get up and running with AIDX in minutes
    ---

    ## Installation

    Install AIDX using your preferred package manager:

    ```bash
    npm install -g aidx
    ```

    ## First Steps

    Run the AIDX CLI to verify your installation:

    ```bash
    aidx --version
    ```

    > [!NOTE]
    > AIDX requires Node.js 18 or later.
    ```

    The `> [!NOTE]` GFM alert syntax is converted to `<Callout>` by the `remark-gfm-alerts-as-callouts` plugin (Phase 2 LIB-06).
  </action>
  <acceptance_criteria>
    - `grep 'Getting Started' docs/nav.json` matches
    - `grep '"path": "/"' docs/nav.json` matches
    - `grep '"path": "/quick-start"' docs/nav.json` matches
    - `grep 'title: AIDX Docs' docs/index.mdx` matches
    - `grep 'hideSidecar: true' docs/index.mdx` matches
    - `grep 'title: Quick Start' docs/quick-start.mdx` matches
    - `grep 'npm install -g aidx' docs/quick-start.mdx` matches
    - `grep '> \[!NOTE\]' docs/quick-start.mdx` matches
  </acceptance_criteria>
  <verify>
    <automated>grep 'Getting Started' docs/nav.json && grep 'hideSidecar: true' docs/index.mdx && grep '> \[!NOTE\]' docs/quick-start.mdx</automated>
  </verify>
  <done>Three docs files exist with correct content. nav.json has one section with two pages. index.mdx has AIDX Docs title and hideSidecar. quick-start.mdx demonstrates headings, code blocks, and a callout.</done>
</task>

<task type="auto">
  <name>Task 2: Copy public assets (favicon and logo SVGs)</name>
  <files>public/favicon.svg, public/aidx-logo.svg</files>
  <read_first>
    .planning/phases/03-pages-docs-build/03-RESEARCH.md
    src/components/navbar/aidx-wordmark.svg
  </read_first>
  <action>
    Create the `public/` directory and copy two SVG files per D-12:

    1. **ASSET-01 (favicon):** Copy `logo-favicon.svg` from repo root to `public/favicon.svg`:
       ```bash
       mkdir -p public
       cp logo-favicon.svg public/favicon.svg
       ```

    2. **ASSET-02 (logo):** Copy `aidx-icon.svg` from repo root to `public/aidx-logo.svg`:
       ```bash
       cp aidx-icon.svg public/aidx-logo.svg
       ```

    3. **ASSET-03 (navbar wordmark):** Already present at `src/components/navbar/aidx-wordmark.svg` from Phase 2. Verify it exists — no action needed.

    Note: D-12 (locked decision) narrows ASSET-01 scope to SVG-only for v1. REQUIREMENTS.md ASSET-01 originally specified favicon.ico + PNG variants, but D-12 overrides this — PNG variants and favicon.ico are explicitly deferred in CONTEXT.md. This plan satisfies ASSET-01 within the D-12 scope.
  </action>
  <acceptance_criteria>
    - `file public/favicon.svg` shows SVG or XML content
    - `file public/aidx-logo.svg` shows SVG or XML content
    - `grep '<svg' public/favicon.svg` matches
    - `grep '<svg' public/aidx-logo.svg` matches
    - `ls src/components/navbar/aidx-wordmark.svg` exists (ASSET-03 verification)
  </acceptance_criteria>
  <verify>
    <automated>grep '<svg' public/favicon.svg && grep '<svg' public/aidx-logo.svg && ls src/components/navbar/aidx-wordmark.svg</automated>
  </verify>
  <done>public/favicon.svg and public/aidx-logo.svg exist as valid SVG files. Navbar wordmark confirmed present from Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Content files are static MDX and JSON. Assets are static SVGs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | docs/*.mdx | accept | Static files committed to git. No runtime modification possible. |
</threat_model>

<verification>
- `ls docs/nav.json docs/index.mdx docs/quick-start.mdx` — all exist
- `ls public/favicon.svg public/aidx-logo.svg` — both exist
- `ls src/components/navbar/aidx-wordmark.svg` — exists (Phase 2)
</verification>

<success_criteria>
All docs content files and public assets are in place. nav.json defines the Getting Started section. MDX files have correct frontmatter. SVG assets are valid.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-docs-build/03-03-SUMMARY.md`
</output>
