# Milestone v1.3: Eye Animation Enhancement

**Status:** ✅ SHIPPED 2026-04-19
**Phases:** 6
**Total Plans:** 2

## Overview

Port the Ghostty "eye becoming round" animation effect to the AIDX homepage character (颜小慧), making her squinting eyes evolve into round circles during the 235-frame animation loop. Implemented as a Node.js post-processor that regenerates all animation frames with a 5-stage eye-opening progression.

## Phases

### Phase 6: Eye Animation Enhancement

**Goal:** Users see the homepage character's eyes evolve from a squinting shape into round circles across the 235-frame animation loop, matching Ghostty's eye-opening visual quality.
**Depends on:** Phase 5
**Plans:** 2 plans

Plans:

- [x] 06-01: Write scripts/generate-eye-frames.js with built-in validator
- [x] 06-02: Run script, verify frame output, add .sort() to HomeContent.tsx, run next build

**Details:**

Requirements: EYE-01 through EYE-12 (all verified complete per 06-VERIFICATION.md)

Success Criteria (all met):
1. Homepage animation shows eyes that start squinting and visibly open into round circles over 235 frames ✓
2. All 235 regenerated frames have identical line lengths — no column drift ✓
3. Both eyes open symmetrically ✓
4. `next build` exits 0 ✓
5. Eye-opening progression matches Ghostty's quality — squint (1–40), slit (41–80), half-open (81–130), round (181–235) ✓

---

## Milestone Summary

**Key Decisions:**

- Validator check 5 was incorrect: eye rows legitimately contain class=o and class=h spans alongside class=e spans; removed the bad-span regex and replaced with a narrower check
- Excluded ghostty/ subtree from TypeScript compilation to unblock build (pre-existing issue)
- Used npm install --no-package-lock --cache /tmp to work around npm 11 Invalid Version bug with sharp 0.34.x

**Issues Resolved:**

- Validator false-positive on legitimate non-eye spans (class="o", class="h") on eye rows
- npm 11 Invalid Version bug with @img/sharp-libvips-* packages
- ghostty/ subtree TypeScript errors blocking build

**Issues Deferred:**

- EYEP-01: Pupil/iris detail inside the round eye (v2)
- EYEP-02: Pre-open blink effect (v2)
- EYEP-03: Staggered eye opening (v2)

**Technical Debt Incurred:**

- REQUIREMENTS.md traceability table was not updated to mark EYE-01 through EYE-12 as complete (all verified via 06-VERIFICATION.md)

---

*For current project status, see .planning/ROADMAP.md*
