---
phase: 02-component-library
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/docs/config.ts
  - src/lib/docs/navigation.ts
  - src/lib/docs/page.ts
  - src/lib/docs/store.ts
  - src/lib/docs/remark-heading-ids.mjs
  - src/lib/docs/remark-gfm-alerts-as-callouts.mjs
  - src/lib/docs/rehype-highlight-all.mjs
  - src/lib/docs/docs-mdx.module.css
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04, LIB-05, LIB-06, LIB-07, LIB-08]

must_haves:
  truths:
    - "config.ts exports DOCS_DIRECTORY, DOCS_PAGES_ROOT_PATH, and GITHUB_REPO_URL with AIDX placeholder URL"
    - "navigation.ts exports loadDocsNavTreeData, navTreeToBreadcrumbs, docsMetadataTitle"
    - "page.ts exports loadDocsPage, loadAllDocsPageSlugs, DocsPageData type, PageHeader type"
    - "store.ts exports useDocsStore zustand store with headerIdsInView, updateHeaderIdInView, resetHeaderIdsInView"
    - "remark-heading-ids.mjs generates stable slugified heading IDs with de-duplication"
    - "remark-gfm-alerts-as-callouts.mjs maps GFM alerts to Callout MDX elements"
    - "rehype-highlight-all.mjs enables full lowlight language set for syntax highlighting"
    - "docs-mdx.module.css provides customMDX class with list, image, hr, footnote styles"
  artifacts:
    - path: "src/lib/docs/config.ts"
      provides: "DOCS_DIRECTORY, DOCS_PAGES_ROOT_PATH, GITHUB_REPO_URL"
      exports: ["DOCS_DIRECTORY", "DOCS_PAGES_ROOT_PATH", "GITHUB_REPO_URL"]
    - path: "src/lib/docs/navigation.ts"
      provides: "loadDocsNavTreeData, navTreeToBreadcrumbs, docsMetadataTitle"
      exports: ["loadDocsNavTreeData", "navTreeToBreadcrumbs", "docsMetadataTitle"]
    - path: "src/lib/docs/page.ts"
      provides: "loadDocsPage, loadAllDocsPageSlugs, DocsPageData, PageHeader"
      exports: ["loadDocsPage", "loadAllDocsPageSlugs", "DocsPageData", "PageHeader"]
    - path: "src/lib/docs/store.ts"
      provides: "useDocsStore zustand store"
      exports: ["useDocsStore"]
    - path: "src/lib/docs/remark-heading-ids.mjs"
      provides: "remarkHeadingIds remark plugin"
    - path: "src/lib/docs/remark-gfm-alerts-as-callouts.mjs"
      provides: "remarkGfmAlertsAsCallouts remark plugin"
    - path: "src/lib/docs/rehype-highlight-all.mjs"
      provides: "rehypeHighlightAll rehype plugin"
    - path: "src/lib/docs/docs-mdx.module.css"
      provides: "customMDX class for MDX content styling"
  key_links:
    - from: "src/lib/docs/navigation.ts"
      to: "src/lib/docs/config.ts"
      via: "imports not direct but shares DOCS_DIRECTORY convention"
      pattern: "docsDirectory"
    - from: "src/lib/docs/rehype-highlight-all.mjs"
      to: "lowlight"
      via: "import { all } from lowlight"
      pattern: "from.*lowlight"
    - from: "src/lib/docs/remark-gfm-alerts-as-callouts.mjs"
      to: "@r4ai/remark-callout"
      via: "import remarkCallout"
      pattern: "from.*remark-callout"
---

<objective>
Create all 8 lib/docs utility modules: config constants, navigation tree loader, page loader, zustand store, three MDX plugins (replacing stubs), and docs MDX styles. Also add missing `lowlight`, `unified`, and `unist-util-visit` as explicit dependencies.

Purpose: These modules are the data layer for the docs system. The zustand store (LIB-04) is a critical dependency for Sidecar and JumplinkHeader in Plan 02-02. The navigation and page loaders are consumed by Phase 3 page routes. The MDX plugins replace the no-op stubs from Phase 1.

Output: 8 lib files (4 new, 3 stub replacements, 1 CSS module) + updated package.json.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-CONTEXT.md
@.planning/phases/02-component-library/02-PATTERNS.md
@.planning/phases/01-project-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Phase 1 stubs to REPLACE (not append to) -->

Current src/lib/docs/remark-heading-ids.mjs (stub):
```js
export default function remarkHeadingIds() {
  return (tree) => tree;
}
```

Current src/lib/docs/remark-gfm-alerts-as-callouts.mjs (stub):
```js
export default function remarkGfmAlertsAsCallouts() {
  return (tree) => tree;
}
```

Current src/lib/docs/rehype-highlight-all.mjs (stub):
```js
export default function rehypeHighlightAll() {
  return (tree) => tree;
}
```

<!-- Types consumed by navigation.ts -->
From src/components/breadcrumbs (will exist after Plan 02-02):
```typescript
export interface Breadcrumb { text: string; href: string | null; }
```

From src/components/nav-tree (will exist after Plan 02-02):
```typescript
export type NavTreeNode = FolderNode | LinkNode | BreakNode;
export interface FolderNode { type: "folder"; title: string; path: string; open: boolean; children: NavTreeNode[]; }
export interface LinkNode { type: "link"; title: string; path: string; active: boolean; }
export interface BreakNode { type: "break"; }
```

NOTE: navigation.ts imports types from @/components/breadcrumbs and @/components/nav-tree.
These components are created in Plan 02-02 (Wave 2). To avoid circular dependency issues,
the executor must either: (a) create minimal type-only stub files for breadcrumbs and nav-tree
that export just the type interfaces, OR (b) inline the types temporarily. Option (a) is preferred
since Plan 02-02 will overwrite those files with full implementations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing dependencies and create config, store, MDX plugins, and docs-mdx styles</name>
  <files>
    package.json
    src/lib/docs/config.ts
    src/lib/docs/store.ts
    src/lib/docs/remark-heading-ids.mjs
    src/lib/docs/remark-gfm-alerts-as-callouts.mjs
    src/lib/docs/rehype-highlight-all.mjs
    src/lib/docs/docs-mdx.module.css
  </files>
  <read_first>
    package.json
    ~/Developments/ghostty-org/website/src/lib/docs/config.ts
    ~/Developments/ghostty-org/website/src/lib/docs/store.ts
    ~/Developments/ghostty-org/website/src/lib/docs/remark-heading-ids.mjs
    ~/Developments/ghostty-org/website/src/lib/docs/remark-gfm-alerts-as-callouts.mjs
    ~/Developments/ghostty-org/website/src/lib/docs/rehype-highlight-all.mjs
    ~/Developments/ghostty-org/website/src/lib/docs/docs-mdx.module.css
    src/lib/docs/remark-heading-ids.mjs
    src/lib/docs/remark-gfm-alerts-as-callouts.mjs
    src/lib/docs/rehype-highlight-all.mjs
  </read_first>
  <action>
1. Add explicit dependencies to package.json `dependencies` object:
   - `"lowlight": "^3.3.0"`
   - `"unified": "^11.0.5"`
   - `"unist-util-visit": "^5.0.0"`
   Then run `npm install`.

2. Create `src/lib/docs/config.ts` — copy verbatim from Ghostty source, change ONE value per D-03:
   ```typescript
   export const DOCS_DIRECTORY = "./docs";
   export const DOCS_PAGES_ROOT_PATH = "/docs";
   export const GITHUB_REPO_URL = "https://github.com/your-org/aidx";
   ```

3. Create `src/lib/docs/store.ts` — copy verbatim from Ghostty source (25 lines). No changes needed:
   ```typescript
   import { create } from "zustand";
   type DocsStoreState = {
     headerIdsInView: string[];
     updateHeaderIdInView: (inView: boolean, id: string) => void;
     resetHeaderIdsInView: () => void;
   };
   export const useDocsStore = create<DocsStoreState>()((set, get) => ({
     headerIdsInView: [],
     updateHeaderIdInView: (inView: boolean, id: string) => {
       const headerIds = get().headerIdsInView;
       if (inView && !headerIds.includes(id)) {
         set({ headerIdsInView: [...headerIds, id] });
       } else {
         set({ headerIdsInView: headerIds.filter((item) => item !== id) });
       }
     },
     resetHeaderIdsInView: () => { set({ headerIdsInView: [] }); },
   }));
   ```

4. REPLACE (not append) `src/lib/docs/remark-heading-ids.mjs` — copy full Ghostty implementation (39 lines). Uses `slugify` and `unist-util-visit`. Generates stable heading IDs with de-duplication via `encounteredIDs` Map.

5. REPLACE (not append) `src/lib/docs/remark-gfm-alerts-as-callouts.mjs` — copy full Ghostty implementation (22 lines). Uses `@r4ai/remark-callout`. Maps GFM alerts to `<Callout>` with `type` and `isFoldable` properties.

6. REPLACE (not append) `src/lib/docs/rehype-highlight-all.mjs` — copy full Ghostty implementation (10 lines). Uses `lowlight` `all` and `rehype-highlight`. Enables full language set with `detect: false`.

7. Create `src/lib/docs/docs-mdx.module.css` — copy verbatim from Ghostty source (92 lines). Contains `.customMDX` class with nested styles for paragraphs, lists, images, hr, sup/footnotes, mermaid containers. Uses `var(--brand-color)`, `var(--gray-*)`, `var(--header-height)` from globals.css. Includes `@keyframes footnote-highlight` and `@keyframes footnote-reference-highlight` animations.
  </action>
  <verify>
    <automated>grep -q "your-org/aidx" src/lib/docs/config.ts && grep -q "useDocsStore" src/lib/docs/store.ts && grep -q "encounteredIDs" src/lib/docs/remark-heading-ids.mjs && grep -q "remarkCallout" src/lib/docs/remark-gfm-alerts-as-callouts.mjs && grep -q "from.*lowlight" src/lib/docs/rehype-highlight-all.mjs && grep -q "customMDX" src/lib/docs/docs-mdx.module.css && grep -q '"lowlight"' package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"lowlight"`, `"unified"`, `"unist-util-visit"` in dependencies
    - npm install completes without errors
    - src/lib/docs/config.ts contains `export const GITHUB_REPO_URL = "https://github.com/your-org/aidx"`
    - src/lib/docs/config.ts contains `export const DOCS_DIRECTORY = "./docs"`
    - src/lib/docs/config.ts contains `export const DOCS_PAGES_ROOT_PATH = "/docs"`
    - src/lib/docs/store.ts contains `export const useDocsStore = create<DocsStoreState>()`
    - src/lib/docs/store.ts contains `headerIdsInView`
    - src/lib/docs/store.ts contains `updateHeaderIdInView`
    - src/lib/docs/remark-heading-ids.mjs contains `import slugify from "slugify"` (NOT the stub `(tree) => tree`)
    - src/lib/docs/remark-heading-ids.mjs contains `encounteredIDs`
    - src/lib/docs/remark-gfm-alerts-as-callouts.mjs contains `import remarkCallout from "@r4ai/remark-callout"` (NOT the stub)
    - src/lib/docs/rehype-highlight-all.mjs contains `import { all } from "lowlight"` (NOT the stub)
    - src/lib/docs/docs-mdx.module.css contains `.customMDX`
    - src/lib/docs/docs-mdx.module.css contains `@keyframes footnote-highlight`
  </acceptance_criteria>
  <done>All 5 standalone lib files created, 3 stubs fully replaced with real implementations, package.json updated with lowlight/unified/unist-util-visit</done>
</task>

<task type="auto">
  <name>Task 2: Create navigation.ts and page.ts with type stub prerequisites</name>
  <files>
    src/components/breadcrumbs/index.tsx
    src/components/nav-tree/index.tsx
    src/lib/docs/navigation.ts
    src/lib/docs/page.ts
  </files>
  <read_first>
    ~/Developments/ghostty-org/website/src/lib/docs/navigation.ts
    ~/Developments/ghostty-org/website/src/lib/docs/page.ts
    ~/Developments/ghostty-org/website/src/components/breadcrumbs/index.tsx
    ~/Developments/ghostty-org/website/src/components/nav-tree/index.tsx
    src/lib/docs/config.ts
  </read_first>
  <action>
1. Create type-only stub `src/components/breadcrumbs/index.tsx` that exports ONLY the `Breadcrumb` interface needed by navigation.ts. Read the full Ghostty breadcrumbs component first, then create a minimal version that exports the type and a placeholder default export. Plan 02-02 will overwrite this file with the full implementation.

   The Breadcrumb type (from Ghostty source):
   ```typescript
   export interface Breadcrumb {
     text: string;
     href: string | null;
   }
   ```
   Include a default export placeholder component so the file is a valid module.

2. Create type-only stub `src/components/nav-tree/index.tsx` that exports the NavTreeNode types needed by navigation.ts. Read the full Ghostty nav-tree component first, then create a minimal version that exports the types and a placeholder default export. Plan 02-02 will overwrite this file with the full implementation.

   The NavTree types (from Ghostty source):
   ```typescript
   export interface FolderNode { type: "folder"; title: string; path: string; open: boolean; children: NavTreeNode[]; }
   export interface LinkNode { type: "link"; title: string; path: string; active: boolean; }
   export interface BreakNode { type: "break"; }
   export type NavTreeNode = FolderNode | LinkNode | BreakNode;
   ```
   Include a default export placeholder component so the file is a valid module.

3. Create `src/lib/docs/navigation.ts` — copy verbatim from Ghostty source (207 lines). Apply these AIDX-specific changes:
   - In `docsMetadataTitle`: change `"Ghostty Docs"` to `"AIDX Docs"` (line 54 of Ghostty source)
   - In `contextualizeNavFile`: change `title: "Ghostty Docs"` to `title: "AIDX Docs"` (line 157 of Ghostty source)
   - All imports remain the same: `@/components/breadcrumbs`, `@/components/nav-tree`, `node:fs`

4. Create `src/lib/docs/page.ts` — copy verbatim from Ghostty source (253 lines). No AIDX-specific changes needed. This file uses:
   - `gray-matter` for frontmatter parsing
   - `remark-mdx`, `remark-parse`, `remark-gfm` for MDX parsing
   - `slugify` for heading ID generation
   - `unified` and `unist-util-visit` for AST traversal
   - `node:fs` and `node:path` for file system access
   - Dynamic MDX import via `await import(nodePath.resolve(relativeFilePath))`
  </action>
  <verify>
    <automated>grep -q "loadDocsNavTreeData" src/lib/docs/navigation.ts && grep -q "AIDX Docs" src/lib/docs/navigation.ts && grep -q "loadDocsPage" src/lib/docs/page.ts && grep -q "loadAllDocsPageSlugs" src/lib/docs/page.ts && grep -q "export interface Breadcrumb" src/components/breadcrumbs/index.tsx && grep -q "export type NavTreeNode" src/components/nav-tree/index.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/breadcrumbs/index.tsx contains `export interface Breadcrumb`
    - src/components/breadcrumbs/index.tsx contains `text: string`
    - src/components/breadcrumbs/index.tsx contains `href: string | null`
    - src/components/nav-tree/index.tsx contains `export interface FolderNode`
    - src/components/nav-tree/index.tsx contains `export interface LinkNode`
    - src/components/nav-tree/index.tsx contains `export interface BreakNode`
    - src/components/nav-tree/index.tsx contains `export type NavTreeNode`
    - src/lib/docs/navigation.ts contains `export async function loadDocsNavTreeData`
    - src/lib/docs/navigation.ts contains `export function navTreeToBreadcrumbs`
    - src/lib/docs/navigation.ts contains `export function docsMetadataTitle`
    - src/lib/docs/navigation.ts contains `"AIDX Docs"` (NOT "Ghostty Docs")
    - src/lib/docs/navigation.ts does NOT contain `Ghostty`
    - src/lib/docs/page.ts contains `export async function loadDocsPage`
    - src/lib/docs/page.ts contains `export async function loadAllDocsPageSlugs`
    - src/lib/docs/page.ts contains `export type PageHeader`
    - src/lib/docs/page.ts contains `export interface DocsPageData`
    - `npx tsc --noEmit` produces no errors in src/lib/docs/ files (type stubs satisfy imports)
  </acceptance_criteria>
  <done>navigation.ts and page.ts created with AIDX branding, type stubs for breadcrumbs and nav-tree satisfy TypeScript imports, all 8 LIB modules complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX content -> remark/rehype plugins | Docs content processed by plugins at build time |
| File system -> page.ts | Reads .mdx files from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | page.ts file reads | accept | Build-time only, reads from local docs/ directory under developer control |
| T-02-02 | I (Info Disclosure) | GITHUB_REPO_URL | accept | Placeholder URL, no secrets; real URL is public when set |
| T-02-03 | T (Tampering) | Terminal dangerouslySetInnerHTML | accept | Frame content is build-time generated from local files, not user input (handled in Plan 02-01) |
</threat_model>

<verification>
After both tasks complete:
1. `grep -r "Ghostty\|ghostty" src/lib/docs/` returns zero matches
2. `grep -q "useDocsStore" src/lib/docs/store.ts` succeeds
3. All 8 lib files exist: `ls src/lib/docs/{config.ts,navigation.ts,page.ts,store.ts,remark-heading-ids.mjs,remark-gfm-alerts-as-callouts.mjs,rehype-highlight-all.mjs,docs-mdx.module.css} | wc -l` equals 8
4. No stub remnants: `grep -l "(tree) => tree" src/lib/docs/*.mjs` returns empty
5. `npx tsc --noEmit` passes for lib files
</verification>

<success_criteria>
All 8 LIB modules (LIB-01 through LIB-08) are implemented, typed, and importable. The three Phase 1 stubs are fully replaced with real implementations. The zustand store is ready for consumption by Sidecar and JumplinkHeader in Plan 02-02. Missing npm dependencies are declared and installed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-03-SUMMARY.md`
</output>