---
phase: 04-animation-color-smoothness-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/terminal/Terminal.module.css
autonomous: true
requirements: [REQ-01]

must_haves:
  truths:
    - "Terminal.module.css contains :global(.e), :global(.g), :global(.h), :global(.o) color rules"
    - "Colors match aidx-landing.html exactly: .e=#7ec8f0, .h=#3A5ECF, .g=#5577dd, .o=#6699ff"
    - "Existing .b rule and all other Terminal styles are unchanged"
  artifacts:
    - path: "src/components/terminal/Terminal.module.css"
      provides: "Terminal styles with all animation frame color classes"
      contains: ":global(.e)"
  key_links: []
---

<objective>
Add the four missing animation frame color classes to Terminal.module.css.

Purpose: The animation frames use span classes `e`, `g`, `h`, `o` for colored ASCII art. Without these rules, all spans render as the default gray text color, making the animation look wrong. This is a 4-line CSS change.
Output: Terminal.module.css with `.e`, `.g`, `.h`, `.o` color rules alongside the existing `.b` rule.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/04-animation-color-smoothness-fix/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animation frame color classes to Terminal.module.css</name>
  <files>src/components/terminal/Terminal.module.css</files>
  <read_first>
    src/components/terminal/Terminal.module.css
    aidx-landing.html
  </read_first>
  <action>
    In `src/components/terminal/Terminal.module.css`, find the `& .content` block's `& > div` section (around line 114–119):

    ```css
    & > div {
      white-space: pre-wrap;
      & :global(.b) {
        color: var(--brand-color);
      }
    }
    ```

    Add the four missing color rules immediately after the existing `.b` rule:

    ```css
    & > div {
      white-space: pre-wrap;
      & :global(.b) {
        color: var(--brand-color);
      }
      & :global(.e) {
        color: #7ec8f0;
      }
      & :global(.h) {
        color: #3A5ECF;
      }
      & :global(.g) {
        color: #5577dd;
      }
      & :global(.o) {
        color: #6699ff;
      }
    }
    ```

    Color values from `aidx-landing.html` lines 22–25:
    - `.e` → `#7ec8f0` (bright cyan highlight)
    - `.h` → `#3A5ECF` (brand blue, same as `--brand-color`)
    - `.g` → `#5577dd` (mid blue)
    - `.o` → `#6699ff` (light blue)

    Do not change any other part of the file.
  </action>
  <acceptance_criteria>
    - `grep ':global(.e)' src/components/terminal/Terminal.module.css` matches
    - `grep ':global(.g)' src/components/terminal/Terminal.module.css` matches
    - `grep ':global(.h)' src/components/terminal/Terminal.module.css` matches
    - `grep ':global(.o)' src/components/terminal/Terminal.module.css` matches
    - `grep '#7ec8f0' src/components/terminal/Terminal.module.css` matches
    - `grep '#3A5ECF' src/components/terminal/Terminal.module.css` matches
    - `grep '#5577dd' src/components/terminal/Terminal.module.css` matches
    - `grep '#6699ff' src/components/terminal/Terminal.module.css` matches
    - `grep ':global(.b)' src/components/terminal/Terminal.module.css` still matches (existing rule preserved)
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>grep ':global(.e)' src/components/terminal/Terminal.module.css && grep ':global(.g)' src/components/terminal/Terminal.module.css && grep ':global(.h)' src/components/terminal/Terminal.module.css && grep ':global(.o)' src/components/terminal/Terminal.module.css && npm run lint</automated>
  </verify>
  <done>Terminal.module.css has all 5 animation frame color classes (.b, .e, .g, .h, .o) as :global() rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Pure CSS change with no user input or external data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No threats — CSS-only change |
</threat_model>

<verification>
- `grep ':global(.e)' src/components/terminal/Terminal.module.css` matches
- `grep ':global(.g)' src/components/terminal/Terminal.module.css` matches
- `grep ':global(.h)' src/components/terminal/Terminal.module.css` matches
- `grep ':global(.o)' src/components/terminal/Terminal.module.css` matches
- `npm run lint` exits with code 0
</verification>

<success_criteria>
Terminal.module.css contains all four new color rules. Lint passes. Animation frames will now render with correct colors matching aidx-landing.html.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.1-phases/04-animation-color-smoothness-fix/04-01-SUMMARY.md`
</output>
