---
phase: 03-pages-docs-build
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
requirements: [BUILD-01, BUILD-02, BUILD-03]

must_haves:
  truths:
    - "npm run build completes with exit code 0"
    - "npm run lint completes with exit code 0"
    - "Build output contains only static HTML (no server-side routes)"
  artifacts: []
  key_links: []
---

<objective>
Verify the full static build passes — the phase gate that proves all pages, docs, and assets are correctly wired.

Purpose: BUILD-01/02/03 are the acceptance gate for Phase 3. If the build passes, the site is a working static export.
Output: Clean build + lint. Any issues found are fixed in-place.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-docs-build/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and lint, fix any issues</name>
  <files></files>
  <read_first>
    .planning/phases/03-pages-docs-build/03-RESEARCH.md
    package.json
    biome.json
    next.config.mjs
  </read_first>
  <action>
    Run the build and lint commands. Fix any issues found.

    1. Run `npm run build` — must complete with exit code 0 (BUILD-01).
       - If build fails, read the error output carefully.
       - Common issues documented in RESEARCH.md Common Pitfalls:
         - PreviewBanner import → remove it (should already be done per D-02)
         - Missing PNG in not-found.tsx → should already use simplified version
         - v2 component imports in mdx-components.tsx → should already use v1 subset
         - nav.json path mismatch → verify paths match MDX file slugs
       - Fix any issues and re-run until build passes.

    2. Run `npm run lint` — must complete with exit code 0 (BUILD-02).
       - If Biome flags issues (e.g., `noImgElement` on plain `<img>` tags), add `// biome-ignore` comments as needed.
       - Fix any issues and re-run until lint passes.

    3. Verify static output (BUILD-03):
       - Check that `src/app/not-found.tsx` has `export const dynamic = "force-static"`
       - Check that `src/app/docs/[[...path]]/page.tsx` has `export const dynamicParams = false`
       - Confirm no API routes exist: `ls src/app/api/ 2>/dev/null` should return nothing
       - Confirm build output has no "server" or "lambda" entries in the route manifest
  </action>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0
    - `grep 'force-static' src/app/not-found.tsx` matches
    - `grep 'dynamicParams = false' "src/app/docs/[[...path]]/page.tsx"` matches
    - `ls src/app/api/ 2>/dev/null` returns empty (no API routes)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Build and lint pass clean. Site is fully static with no SSR or API routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all pages</name>
  <files></files>
  <action>
    Human visually verifies all pages render correctly in the browser. Run `npm run dev` and check each page.
  </action>
  <acceptance_criteria>
    - Homepage shows animated terminal with "> AIDX" title
    - Homepage shows tagline and two CTA buttons
    - Homepage has no navbar or footer
    - /docs shows sidebar with "Getting Started" section
    - /docs/quick-start shows headings, code blocks, and NOTE callout
    - /nonexistent shows 404 page with navbar and footer
  </acceptance_criteria>
  <what-built>Complete AIDX website with homepage (animated terminal hero, tagline, CTAs), docs pages (/docs with sidebar nav, /docs/quick-start with code blocks and callout), and 404 page.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in your terminal
    2. Visit http://localhost:3000 — verify:
       - Animated terminal plays with "> AIDX" title bar
       - Tagline reads "AI-powered Developer Experience Platform. Accelerate your development workflow with intelligent tools."
       - Two CTA buttons: "Get Started" and "Documentation"
       - No navbar or footer on homepage
    3. Click "Documentation" button or visit http://localhost:3000/docs — verify:
       - Navbar appears with "Docs" and "GitHub" links
       - Sidebar shows "Getting Started" section with "Overview" and "Quick Start"
       - Content shows "Welcome to the AIDX documentation"
       - No sidecar TOC (hideSidecar: true on index page)
    4. Click "Quick Start" in sidebar or visit http://localhost:3000/docs/quick-start — verify:
       - Page renders with headings, code blocks with syntax highlighting, and a NOTE callout
       - Sidecar TOC appears on the right showing "Installation" and "First Steps"
    5. Visit http://localhost:3000/nonexistent — verify:
       - 404 page shows "This page could not be found." with link to docs
       - Navbar and footer are present
  </how-to-verify>
  <verify>
    <automated>echo "Human verification — no automated check"</automated>
  </verify>
  <done>Human confirms all pages render correctly.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Build verification only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No threats — build verification only |
</threat_model>

<verification>
- `npm run build` exit code 0
- `npm run lint` exit code 0
- Homepage renders with terminal animation
- Docs pages render with sidebar and content
- 404 page renders correctly
</verification>

<success_criteria>
Build and lint pass clean. Human verifies all pages render correctly in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-docs-build/03-04-SUMMARY.md`
</output>
