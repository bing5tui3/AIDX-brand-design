---
phase: 01-project-scaffold
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/styles/globals.css
  - src/types/style.ts
  - src/components/text/font/pretendard-std-variable.woff2
  - src/lib/docs/remark-heading-ids.mjs
  - src/lib/docs/remark-gfm-alerts-as-callouts.mjs
  - src/lib/docs/rehype-highlight-all.mjs
autonomous: true
requirements: [STYLE-01, STYLE-02, STYLE-03, FONT-01, FONT-02, LIB-09]
must_haves:
  truths:
    - "CSS custom properties --brand-color, --gray-0 through --gray-9, --atom-one-* are defined in globals.css"
    - "Print media query inverts gray scale and atom-one colors"
    - ".hljs-* syntax highlighting classes are defined globally"
    - "Pretendard woff2 font file exists at src/components/text/font/"
    - "Unit, UnitProp, SpacingProp types are exported from src/types/style.ts"
    - "npm run dev boots without MODULE_NOT_FOUND errors"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "All CSS custom properties and syntax highlight classes"
      contains: "--brand-color: #3A5ECF"
    - path: "src/types/style.ts"
      provides: "Unit, UnitProp, SpacingProp type exports"
      exports: ["Unit", "UnitProp", "SpacingProp"]
    - path: "src/components/text/font/pretendard-std-variable.woff2"
      provides: "Local Pretendard font file"
    - path: "src/lib/docs/remark-heading-ids.mjs"
      provides: "No-op remark plugin stub"
      exports: ["default"]
    - path: "src/lib/docs/remark-gfm-alerts-as-callouts.mjs"
      provides: "No-op remark plugin stub"
      exports: ["default"]
    - path: "src/lib/docs/rehype-highlight-all.mjs"
      provides: "No-op rehype plugin stub"
      exports: ["default"]
  key_links:
    - from: "next.config.mjs"
      to: "src/lib/docs/remark-heading-ids.mjs"
      via: "require.resolve()"
      pattern: "require\\.resolve.*remark-heading-ids"
    - from: "next.config.mjs"
      to: "src/lib/docs/remark-gfm-alerts-as-callouts.mjs"
      via: "require.resolve()"
      pattern: "require\\.resolve.*remark-gfm-alerts"
    - from: "next.config.mjs"
      to: "src/lib/docs/rehype-highlight-all.mjs"
      via: "require.resolve()"
      pattern: "require\\.resolve.*rehype-highlight-all"
---

<objective>
Create the design token layer (globals.css with full CSS custom properties, syntax highlight classes, print mode), shared types (style.ts), font file (Pretendard woff2), and MDX plugin stubs so `npm run dev` boots cleanly.

Purpose: Establish the visual foundation that Phase 2 components consume, and unblock the dev server by providing the three plugin files next.config.mjs references.
Output: globals.css with all tokens, style.ts types, font file, 3 plugin stubs.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold/01-CONTEXT.md
@.planning/phases/01-project-scaffold/01-RESEARCH.md
@.planning/phases/01-project-scaffold/01-PATTERNS.md
@.planning/phases/01-project-scaffold/01-UI-SPEC.md
@.planning/phases/01-project-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — config files the executor needs to know exist -->
next.config.mjs references these three files via require.resolve():
- ./src/lib/docs/remark-gfm-alerts-as-callouts.mjs
- ./src/lib/docs/remark-heading-ids.mjs
- ./src/lib/docs/rehype-highlight-all.mjs
These MUST exist or `next dev` throws MODULE_NOT_FOUND.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create globals.css with full token set and syntax highlight classes</name>
  <files>src/styles/globals.css</files>
  <read_first>
    /Users/ruiwang/Developments/ghostty-org/website/src/styles/globals.css
    /Users/ruiwang/Developments/webank/aidx/.planning/phases/01-project-scaffold/01-PATTERNS.md
    /Users/ruiwang/Developments/webank/aidx/.planning/phases/01-project-scaffold/01-UI-SPEC.md
  </read_first>
  <action>
Copy the entire Ghostty globals.css (178 lines) verbatim to src/styles/globals.css with exactly TWO value changes (per D-03, STYLE-01):

Line 33: `--brand-color: #3551F3` becomes `--brand-color: #3A5ECF`
Line 34: `--brand-color-hsl: 240 100% 45%` becomes `--brand-color-hsl: 226 61% 52%`

Everything else copies exactly:
- Box-sizing reset (lines 1-5)
- ::selection with color-mix (lines 7-9)
- html color-scheme dark (lines 11-13)
- html,body max-width/background (lines 15-19)
- body.noScroll (lines 21-23)
- html,body token block (lines 25-68): --header-height 72px, --footer-height 87px, gray scale --gray-0 (#0F0F11) through --gray-9 (#F0F0F4), --black #000, --white #fff, --form-border-radius 6px, --form-box-shadow, atom-one colors (blue #61aeee, green #98c379, yellow #e6c07b, purple #c678dd, red #e06c75, orange #d19a66, teal #56b6c2, gray #5c6370, white #abb2bf), callout aliases (note=blue, tip=green, important=purple, warning=yellow, caution=red)
- pre code block styling (lines 70-77)
- All .hljs-* classes (lines 79-146): comment/quote, doctag/keyword/formula, section/name/selector-tag/deletion/subst, literal, string/regexp/addition/attribute/meta-string, built_in/class.title, attr/variable/template-variable/type/selector-class/selector-attr/selector-pseudo/number, symbol/bullet/link/meta/selector-id/title, emphasis, strong, link underline
- @media print block (lines 148-177): inverted gray scale (gray-0 #ededf1 through gray-9 #08080d), black=#ffffff, white=#000000, background white, print-safe atom-one colors (blue #0e5c99, green #4e6a24, yellow #805215, purple #8b2085, red #a11b2c, orange #8b553a, teal #2b959d, gray #8f97a3, white #434958)
  </action>
  <verify>
    <automated>cd /Users/ruiwang/Developments/webank/aidx && grep -c "brand-color: #3A5ECF" src/styles/globals.css && grep -c "brand-color-hsl: 226 61% 52%" src/styles/globals.css && grep -c "hljs-" src/styles/globals.css && grep -c "@media print" src/styles/globals.css && grep -c "\-\-gray-0" src/styles/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/globals.css contains `--brand-color: #3A5ECF` (NOT #3551F3)
    - src/styles/globals.css contains `--brand-color-hsl: 226 61% 52%` (NOT 240 100% 45%)
    - src/styles/globals.css contains `--gray-0: #0F0F11`
    - src/styles/globals.css contains `--gray-9: #F0F0F4`
    - src/styles/globals.css contains `.hljs-comment` class
    - src/styles/globals.css contains `.hljs-keyword` class
    - src/styles/globals.css contains `@media print` block with inverted gray scale
    - src/styles/globals.css contains `--callout-note: var(--atom-one-blue)`
    - src/styles/globals.css contains `::selection` with `color-mix`
  </acceptance_criteria>
  <done>globals.css has all CSS custom properties (brand color, gray scale, atom-one, callouts, layout tokens), all .hljs-* syntax highlight classes, and print media query — matching Ghostty verbatim except the two brand color values.</done>
</task>

<task type="auto">
  <name>Task 2: Create style types, copy font file, and create MDX plugin stubs</name>
  <files>src/types/style.ts, src/components/text/font/pretendard-std-variable.woff2, src/lib/docs/remark-heading-ids.mjs, src/lib/docs/remark-gfm-alerts-as-callouts.mjs, src/lib/docs/rehype-highlight-all.mjs</files>
  <read_first>
    /Users/ruiwang/Developments/ghostty-org/website/src/types/style.ts
    /Users/ruiwang/Developments/webank/aidx/.planning/phases/01-project-scaffold/01-PATTERNS.md
  </read_first>
  <action>
1. Create `src/types/style.ts` with exact content from Ghostty (per LIB-09):
```typescript
export type Unit = "px" | "em";
export type UnitProp = `${number}${Unit}` | 0;
export type SpacingProp =
  | UnitProp
  | `${UnitProp} ${UnitProp}`
  | `${UnitProp} ${UnitProp} ${UnitProp}`
  | `${UnitProp} ${UnitProp} ${UnitProp} ${UnitProp}`;
```

2. Copy Pretendard font file (per D-04, FONT-01):
```bash
cp /Users/ruiwang/Developments/ghostty-org/website/src/components/text/font/pretendard-std-variable.woff2 \
   src/components/text/font/pretendard-std-variable.woff2
```

3. Create three MDX plugin stubs (required by next.config.mjs require.resolve calls):

`src/lib/docs/remark-heading-ids.mjs`:
```javascript
export default function remarkHeadingIds() {
  return (tree) => tree;
}
```

`src/lib/docs/remark-gfm-alerts-as-callouts.mjs`:
```javascript
export default function remarkGfmAlertsAsCallouts() {
  return (tree) => tree;
}
```

`src/lib/docs/rehype-highlight-all.mjs`:
```javascript
export default function rehypeHighlightAll() {
  return (tree) => tree;
}
```

4. FONT-02 (JetBrains Mono from Google Fonts): No file needed in Phase 1. The font is loaded via `next/font/google` in Phase 3's layout.tsx. Phase 1 only ensures the dependency (`next`) is installed, which Plan 01 handles.

5. Verify `npm run dev` boots without MODULE_NOT_FOUND by starting and immediately stopping the dev server:
```bash
cd /Users/ruiwang/Developments/webank/aidx && timeout 15 npx next dev --port 3099 2>&1 || true
```
Look for "Ready" or "compiled" in output (success) vs "MODULE_NOT_FOUND" (failure).
  </action>
  <verify>
    <automated>cd /Users/ruiwang/Developments/webank/aidx && test -f src/types/style.ts && grep -q "UnitProp" src/types/style.ts && echo "style.ts OK" && test -f src/components/text/font/pretendard-std-variable.woff2 && echo "font OK" && test -f src/lib/docs/remark-heading-ids.mjs && test -f src/lib/docs/remark-gfm-alerts-as-callouts.mjs && test -f src/lib/docs/rehype-highlight-all.mjs && echo "stubs OK" && node -e "require('./src/lib/docs/remark-heading-ids.mjs')" 2>/dev/null; grep -q "export default function" src/lib/docs/remark-heading-ids.mjs && echo "stub exports OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/types/style.ts contains `export type Unit = "px" | "em"`
    - src/types/style.ts contains `export type UnitProp`
    - src/types/style.ts contains `export type SpacingProp`
    - src/components/text/font/pretendard-std-variable.woff2 exists and is non-empty (binary font file)
    - src/lib/docs/remark-heading-ids.mjs contains `export default function remarkHeadingIds`
    - src/lib/docs/remark-gfm-alerts-as-callouts.mjs contains `export default function remarkGfmAlertsAsCallouts`
    - src/lib/docs/rehype-highlight-all.mjs contains `export default function rehypeHighlightAll`
    - Each stub returns `(tree) => tree`
  </acceptance_criteria>
  <done>style.ts exports Unit/UnitProp/SpacingProp types. Pretendard woff2 copied from Ghostty source. Three MDX plugin stubs exist so next.config.mjs require.resolve() calls succeed. Dev server boots without MODULE_NOT_FOUND.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file copy → project | Font file copied from trusted local Ghostty clone |
| CSS tokens → browser | Custom properties consumed by browser rendering engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | globals.css custom properties | accept | CSS tokens are static values with no user input; no injection vector |
| T-01-05 | Tampering | MDX plugin stubs | accept | Stubs are no-op identity functions; full implementations in Phase 2 will be reviewed then |
| T-01-06 | Information Disclosure | Font file | accept | Pretendard is an open-source font (OFL license); no sensitive data |
</threat_model>

<verification>
1. `grep "brand-color: #3A5ECF" src/styles/globals.css` returns a match
2. `grep "UnitProp" src/types/style.ts` returns a match
3. `test -f src/components/text/font/pretendard-std-variable.woff2` exits 0
4. All three .mjs stubs exist and contain `export default function`
5. `npm run dev` starts without MODULE_NOT_FOUND (may show "page not found" since no pages exist yet — that's expected)
</verification>

<success_criteria>
- globals.css defines all CSS custom properties with AIDX brand color #3A5ECF
- Print media query inverts colors
- .hljs-* classes defined for syntax highlighting
- style.ts exports Unit, UnitProp, SpacingProp
- Pretendard font file exists
- Dev server boots without module resolution errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold/01-02-SUMMARY.md`
</output>
