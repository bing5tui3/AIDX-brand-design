---
phase: 03-pages-docs-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/layout.module.css
  - src/app/page.tsx
  - src/app/HomeContent.tsx
  - src/app/home-content.module.css
  - src/app/terminal-data.tsx
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "Root layout renders html/body with Pretendard and JetBrains Mono font classes"
    - "Navbar and Footer are excluded on the homepage via PathnameFilter"
    - "Homepage loads terminal animation frames at build time and passes them to HomeContent"
    - "HomeContent renders AnimatedTerminal with title '> AIDX', tagline, and two CTA buttons"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, navbar, footer, metadata"
      exports: ["AppLayout", "metadata"]
    - path: "src/app/layout.module.css"
      provides: "Root layout styles"
      contains: ".rootLayout"
    - path: "src/app/page.tsx"
      provides: "Homepage server component"
      exports: ["HomePage", "metadata"]
    - path: "src/app/HomeContent.tsx"
      provides: "Homepage client component with terminal + CTAs"
      contains: "use client"
    - path: "src/app/home-content.module.css"
      provides: "Homepage styles"
      contains: ".homePage"
    - path: "src/app/terminal-data.tsx"
      provides: "loadAllTerminalFiles server utility"
      exports: ["loadAllTerminalFiles"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "@/components/navbar"
      via: "import Navbar"
      pattern: "import Navbar from"
    - from: "src/app/page.tsx"
      to: "src/app/HomeContent.tsx"
      via: "import HomeContent"
      pattern: "import HomeContent"
    - from: "src/app/page.tsx"
      to: "src/app/terminal-data.tsx"
      via: "import loadAllTerminalFiles"
      pattern: "loadAllTerminalFiles"
---

<objective>
Create the root layout and homepage — the two top-level app pages that wire Phase 2 components into a working site.

Purpose: Establishes the app shell (fonts, navbar, footer, metadata) and the homepage hero (animated terminal, tagline, CTAs). Every other page inherits the root layout.
Output: 6 files in src/app/ — the site boots and renders the homepage.
</objective>

<execution_context>
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ruiwang/Developments/webank/aidx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pages-docs-build/03-PATTERNS.md
@.planning/phases/03-pages-docs-build/03-RESEARCH.md

<interfaces>
<!-- Phase 2 exports consumed by this plan -->

From src/components/text/index.tsx:
```typescript
export const pretendardStdVariable: NextFont;
export const jetbrainsMono: NextFont;
export { H1, H2, H3, H4, H5, H6, P, Span, Code, LI, BodyParagraph };
```

From src/components/link/index.tsx:
```typescript
export type SimpleLink = { text: string; href: string };
export default function ButtonLink(props: { href: string; text: string; size?: "small" | "medium" | "large"; theme?: "brand" | "neutral" }): JSX.Element;
```

From src/lib/docs/config.ts:
```typescript
export const DOCS_DIRECTORY = "./docs";
export const DOCS_PAGES_ROOT_PATH = "/docs";
export const GITHUB_REPO_URL = "https://github.com/your-org/aidx";
```

From src/lib/docs/navigation.ts:
```typescript
export async function loadDocsNavTreeData(docsDir: string, activePath: string): Promise<NavTreeNode[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create terminal-data utility and homepage CSS modules</name>
  <files>src/app/terminal-data.tsx, src/app/home-content.module.css, src/app/layout.module.css</files>
  <read_first>
    ~/Developments/ghostty-org/website/src/app/terminal-data.tsx
    ~/Developments/ghostty-org/website/src/app/home-content.module.css
    ~/Developments/ghostty-org/website/src/app/layout.module.css
    .planning/phases/03-pages-docs-build/03-PATTERNS.md
  </read_first>
  <action>
    Copy these three files verbatim from the Ghostty source — no AIDX-specific changes needed for any of them:

    1. `src/app/terminal-data.tsx` — Copy verbatim from `~/Developments/ghostty-org/website/src/app/terminal-data.tsx`. Exports `loadAllTerminalFiles` and type `TerminalsMap`. Reads from `./terminals` directory.

    2. `src/app/home-content.module.css` — Copy verbatim from `~/Developments/ghostty-org/website/src/app/home-content.module.css`. Classes: `.homePage`, `.terminalWrapper`, `.tagline`, `.buttonsList`, `.animatedTerminal`.

    3. `src/app/layout.module.css` — Copy verbatim from `~/Developments/ghostty-org/website/src/app/layout.module.css`. Class: `.rootLayout` with table styling.

    Per D-01: copy verbatim strategy, no rewrites.
  </action>
  <acceptance_criteria>
    - `grep -c "loadAllTerminalFiles" src/app/terminal-data.tsx` returns >= 1
    - `grep -c "TerminalsMap" src/app/terminal-data.tsx` returns >= 1
    - `grep -c ".homePage" src/app/home-content.module.css` returns >= 1
    - `grep -c ".tagline" src/app/home-content.module.css` returns >= 1
    - `grep -c ".buttonsList" src/app/home-content.module.css` returns >= 1
    - `grep -c ".rootLayout" src/app/layout.module.css` returns >= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "loadAllTerminalFiles" src/app/terminal-data.tsx && grep -c ".homePage" src/app/home-content.module.css && grep -c ".rootLayout" src/app/layout.module.css</automated>
  </verify>
  <done>All three utility/style files exist with correct exports and class names</done>
</task>

<task type="auto">
  <name>Task 2: Create root layout, homepage page, and HomeContent client component</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/HomeContent.tsx</files>
  <read_first>
    ~/Developments/ghostty-org/website/src/app/layout.tsx
    ~/Developments/ghostty-org/website/src/app/page.tsx
    ~/Developments/ghostty-org/website/src/app/HomeContent.tsx
    .planning/phases/03-pages-docs-build/03-PATTERNS.md
    src/app/terminal-data.tsx
    src/app/layout.module.css
    src/app/home-content.module.css
    src/components/text/index.tsx
    src/components/navbar/index.tsx
    src/components/footer/index.tsx
    src/components/pathname-filter/index.tsx
    src/components/link/index.tsx
    src/components/animated-terminal/index.tsx
    src/components/grid-container/index.tsx
    src/lib/docs/config.ts
    src/lib/docs/navigation.ts
  </read_first>
  <action>
    Create three files by copying from Ghostty source with documented AIDX deltas:

    **src/app/layout.tsx** — Copy from Ghostty `layout.tsx`, apply these deltas:
    - REMOVE `import PreviewBanner from "@/components/preview-banner"` and its JSX `<PreviewBanner />` (per D-02)
    - navLinks: `[{ text: "Docs", href: "/docs" }, { text: "GitHub", href: "https://github.com/your-org/aidx" }]` (per D-10, no Discord)
    - `NO_CHROME_PATHS = ["/"]`
    - metadata: `title: "AIDX"`, `description: "AI-powered Developer Experience Platform"`, `metadataBase: new URL("https://example.com")` (per D-11)
    - icons: `{ icon: [{ url: "/favicon.svg", type: "image/svg+xml" }] }` — no PNG, no shortcut (per D-12)
    - Remove `openGraph.images` entirely (per D-13)
    - Footer copyright: `` `© ${currentYear} AIDX` ``
    - CTA in Navbar: `{ href: "#", text: "Get Started" }` (per D-04)

    **src/app/page.tsx** — Copy from Ghostty `page.tsx`, apply:
    - metadata title: `"AIDX"`, description: `"AI-powered Developer Experience Platform"` (per D-11)
    - Keep `loadAllTerminalFiles("/home")` and `<HomeContent terminalData={terminalData} />` verbatim

    **src/app/HomeContent.tsx** — Copy from Ghostty `HomeContent.tsx`, apply:
    - `"use client"` directive at top
    - Terminal title: `"> AIDX"` (per D-05)
    - Tagline: `"AI-powered Developer Experience Platform. Accelerate your development workflow with intelligent tools."` (per D-03)
    - CTA buttons: `<ButtonLink href="#" text="Get Started" size="large" />` + `<ButtonLink href="/docs" text="Documentation" size="large" theme="neutral" />` (per D-04)
    - Keep `useWindowSize` hook and responsive font size logic verbatim from Ghostty
  </action>
  <acceptance_criteria>
    - `grep -c "PreviewBanner" src/app/layout.tsx` returns 0
    - `grep 'title: "AIDX"' src/app/layout.tsx` matches
    - `grep 'description: "AI-powered Developer Experience Platform"' src/app/layout.tsx` matches
    - `grep '/favicon.svg' src/app/layout.tsx` matches
    - `grep -c "openGraph" src/app/layout.tsx` returns 0
    - `grep '© .* AIDX' src/app/layout.tsx` matches
    - `grep 'NO_CHROME_PATHS' src/app/layout.tsx` matches
    - `grep '"use client"' src/app/HomeContent.tsx` matches
    - `grep '> AIDX' src/app/HomeContent.tsx` matches
    - `grep 'Accelerate your development workflow' src/app/HomeContent.tsx` matches
    - `grep 'Get Started' src/app/HomeContent.tsx` matches
    - `grep 'Documentation' src/app/HomeContent.tsx` matches
    - `grep 'loadAllTerminalFiles' src/app/page.tsx` matches
    - `grep 'HomeContent' src/app/page.tsx` matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c "PreviewBanner" src/app/layout.tsx | grep "^0$" && grep 'title: "AIDX"' src/app/layout.tsx && grep '"use client"' src/app/HomeContent.tsx && grep '> AIDX' src/app/HomeContent.tsx && grep 'loadAllTerminalFiles' src/app/page.tsx</automated>
  </verify>
  <done>Root layout renders with AIDX metadata, no PreviewBanner, SVG favicon only. Homepage loads terminal frames and renders HomeContent with AIDX tagline and CTAs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan. All files are static server components or client components with no user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | layout.tsx metadata | accept | Static metadata only; no secrets. metadataBase is a placeholder URL. |
</threat_model>

<verification>
- All 6 files exist in src/app/
- `grep -r "PreviewBanner" src/app/` returns no matches
- `grep -r "openGraph" src/app/layout.tsx` returns no matches
- `grep "> AIDX" src/app/HomeContent.tsx` returns a match
</verification>

<success_criteria>
Root layout and homepage files are in place. Layout has AIDX metadata, SVG favicon, no PreviewBanner, no OG images. Homepage renders AnimatedTerminal with "> AIDX" title, AIDX tagline, and Get Started + Documentation CTAs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pages-docs-build/03-01-SUMMARY.md`
</output>
